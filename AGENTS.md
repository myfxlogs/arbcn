# arbcn — AI 协作契约（无损交接 SSOT）

> **唯一真相源。** 本文件是所有 AI agent（Claude / Windsurf / Trae / 任何）共同遵守的契约。
> 各工具入口壳（CLAUDE.md / .windsurfrules）只做一件事：加载本文件。**冲突时以本文件为准。**

## 0. 角色与决策权

| 角色 | 职责 | 决策权 |
|------|------|--------|
| **Claude** | 项目第一负责人 · 经营第一负责人 · 唯一决策者 | **最高**：最终技术判断、方案纠错、架构/合规/方向 |
| **其他 agent** | 施工方（码农）：按 AGENTS.md + STATE.md + decisions.md 落地实现 | **无**：不决策；遇设计疑问回找 Claude，不自行变更设计 |
| **人类（甲方业主）** | 只提需求，不出结论 | 需求可能错误 → 由 Claude 以技术判断把关或纠错 |

- 流程：**讨论 → 决定 → 执行**，顺序不可跳。
- Claude 对最终质量负责：不达标不交付；业主方案错误时指出并给更优解，不盲从。

## 1. 定位（P5 方向锚点 · 修改须决策记录）

> **D-008 正式化（2026-08-15）**：arbcn = 个人资金运营决策系统（业主自有 20 万 RMB，多币种 RMB/USD/HKD，可扩展加密）。
> **套利第一性原则（D-019）**：**不赌**——不做无对冲的方向性敞口；收益来自价差收敛与息差，不来自方向判断。裸敞口的收益是风险补偿，不是套利。
> **目标**：长期年化 5–8%（市场条件允许时），最大回撤 ≤ 8%（中风险）；诚实基线 ~3.2–3.7%（D-026 定稿：保本凸性 50% + 稳定币基档 50%），地板 +2.15%，牛市右尾 8–10%+。
> **路径**：**机会监控先行（动态资本路由引擎，D-020/D-022/D-024）**→ 收益阶梯三档（稳定币基档 4.5–6% / sUSDe 类 8–12% / funding 窗口 15–30%）+ 国内底仓（现金管理+民营定期+时点逆回购）+ 持有层单列（TRX/BNB 等，业主自选）→ 台账/归因 v2。优化原则：收益最大 × 路径最短；审计原则：先核实再采纳。
> **形态**：决策监控 + 记账（不自动执行，人工下单）。
> **合规姿态**：国内合法优先；灰色加密层由业主知情承担（42 号文：民事无效、损失自担、法律不保护），激活须过 D-013/D-016 门禁。
> 首行是方向锚，改动必须走 decisions.md（D#）留痕，禁止顺手改。

## 2. 开工前必读（boot · 每次会话第一件事）

1. 本文件全文（T0 契约）。
2. `docs/handoff/STATE.md` — 当前状态 + 交接负载（无损接手核心）。
3. 任务相关的 T1 文档（按需，见 §9 文档索引）。
4. 若自己私有记忆里积累过本项目事实 → 先按 §6.2 上翻到共享层，再开工。
5. `~/kb/INDEX.md`（跨项目知识库，§10）— grep 与任务相关关键词；命中先读条目再动手。

## 3. 无损交接 · 六条第一性原则（设计依据，勿违背）

| # | 原则 | 推论（落地） |
|---|------|------|
| P1 | **通道不变量**：交接只能走双方都读得到的通道；任何 agent 私有记忆对对方不可见 = 对项目不存在 | 项目事实只存 git 跟踪纯文本文档；私有记忆 = 一次性缓存 |
| P2 | **成本不变量**：项目变老，交接成本必须恒定 | 活跃层硬封顶；历史机械滚出活跃层（git 保留，token 不付） |
| P3 | **单一真相源**：一事实存两份，必有一份错 | 每事实恰好一处，其余用指针（file:line / D# / commit） |
| P4 | **可检查性**：无法机械检查的规则必被违反 | 风格/方法编码为门禁（hook / 自审清单） |
| P5 | **方向锚定**：方向只在会话上下文即换会话丢失 | charter 落盘不可变锚，改动留痕 |
| P6 | **增量优先**：交接交付 delta，不重述全量 | 交接负载是固定格式结构化增量，不是叙事 |

## 4. 分层与 token 预算（P2 落地）

| 层 | 文档 | 读取 | 预算 |
|----|------|------|------|
| **T0 契约** | `AGENTS.md` + `STATE.md` | 每次开工必读 | ≤ 20KB / 文件（≈5K token） |
| **T1 知识** | `decisions.md` / `practices.md` / `facts.md` / `dialogue.md` / `docs/README.md` | 按需读 | ≤ 450 行；超量滚动归档 |
| **T2 归档** | `LOG.md` | 永不进上下文，git 追溯 | 不限 |

- **STATE 滑动窗口**：只存「施工表 + 阻塞/待决策 + 交接负载」；每完成一任务，最旧 ✅ 机械移 `LOG.md`。
- 规则：表格 > 散文；指针 > 复制；不重述历史；不引代码块（给 file:line）。

## 5. 交接负载（STATE.md 固定块 · 每次覆盖 · git 留痕）

```
## 交接负载
- 现状:      <一句话>
- 方向校验:  <与 §1 定位一致？一句话证明没漂移>
- 施工表:    | 子任务 | ⬜🔄✅ | 锚点(D#/commit/file:line) |
- 阻塞/待决策: <必须显式，不埋自由文本>
- 下一步:    <第一条可执行动作>
- 清扫上翻:  <本次从私有记忆翻到共享层的条目>
```

## 6. 启动 / 收工协议（无差别强制）

### 6.1 boot（开工）
读 §2 清单 → 带交接负载开工。**未读 STATE.md 就动手 = 失职。**

### 6.2 私有记忆清扫（check-out 强制项）
收工前扫描自己私有记忆：找出「对项目有用、尚未落盘」的事实（决策/约束/坑/风格教训）→ 上翻到对应 T1 文件 → 确认共享层无缺 → 私有记忆可丢弃。
**私有记忆里不许留项目事实的唯一副本（P1）。**

### 6.3 check-out（收工）
1. 通过 §7.3 自审 → 2. 更新 STATE.md（施工表/滑动窗口/阻塞显式）→ 3. 私有记忆清扫（§6.2）→ 4. 本次对话落 `dialogue.md`（§8）→ 5. 通过 §7.2 Before Commit → 6. commit（含 STATE.md）。
**收工不写 STATE.md / dialogue.md = 失职。未提交 = 施工中断信号。**

## 7. 门禁与提交纪律

### 7.1 环境
- git：`git config core.hooksPath scripts/hooks`（每个 clone 一次，已配置）。
- 禁 `--no-verify` 绕过 hook。

### 7.2 Before Commit
- `scripts/hooks/pre-commit` 全过（STATE.md 随提交更新 / 保护文件历史标记未删）。
- diff 通读：无死代码、无 TODO、无调试残留。

### 7.3 交付自审（A–F，任意 agent 无差别）
- **两时点强制**：自审分两时点——① 施工前：设计文档完成即先做 A–F（自行施工或分发施工都须，先审再动，避免完工后审计出错误又返工）；② 交付前：成品再行 A–F。自审是流程不是汇报材料，不待业主追问。
- **A 架构**：复用已有、无逆向依赖；存在更简单等价方案而未采用 = 不达标。
- **B 实现**：第一性——直接对应问题本质，无多余间接层。
- **C 洁净**：无重复 / 死代码 / TODO / 注释掉的代码。
- **D 正确性**：边界 / nil / 竞态 / 错误路径；对抗测试（删关键行测试必红）。
- **E 合规**：§1 定位 + 硬约束逐条对照。
- **F 文档**：改动同步 decisions / practices / dialogue；与代码一致。

### 7.4 root-cause-first
功能消失/行为退化 → **先查 git log + blame，理解原设计意图，再动手**。禁止不查历史直接"重写一个"。换 agent 后最典型的毁项目方式是凭"我觉得更好"重写——本条禁止。

## 8. 对话纪要（dialogue.md · 有痕可阅）

- 每次与业主/agent 的实质对话 → `docs/handoff/dialogue.md` 追加一段结构化纪要（日期/参与方/议题/需求/结论/决策号）。
- 目的：任何 agent 可追溯"为什么这么做"；业主可回看。**全量原始对话由各工具自身 session 记录兜底（人类可读），共享层只落实质纪要（省 token）。**
- dialogue.md 超 450 行 → 最旧段机械移 `LOG.md`（留索引行）。

## 9. 文档索引

| 文档 | 层 | 用途 |
|------|----|------|
| `AGENTS.md`（本文件） | T0 | 契约 SSOT |
| `docs/handoff/STATE.md` | T0 | 当前状态 + 交接负载（开工读 / 收工写） |
| `docs/handoff/decisions.md` | T1 | 决策记录（D#，ADR-lite） |
| `docs/handoff/practices.md` | T1 | 共享实践记忆（高频打回模式 + 风格约定） |
| `docs/handoff/facts.md` | T1 | 市场事实库（值/核实时间/来源/状态；决策前先查库，D-028） |
| `docs/handoff/dialogue.md` | T1 | 对话纪要（有痕可阅） |
| `docs/handoff/LOG.md` | T2 | 历史归档（只存不读，git 追溯） |
| `docs/README.md` | T1 | 文档地图 + 阅读顺序 |
| `CLAUDE.md` / `.windsurfrules` | — | 各工具入口壳 → 本文件 |

## 10. 跨项目知识库（共享 · 任意 agent 无差别）

> 本仓之外，所有项目共享一份踩坑库/模式库。**规则全文见 `~/kb/AGENTS.md`**（库自述，维护者必读）。

- **库位置**：`~/kb`（GitHub 私有仓库 `myfxlogs/kb`，**所有服务器固定路径克隆**——指令文本因此可原样复制）。
- **开工前（强制）**：`cd ~/kb && git pull --rebase --autostash`（clone 只在每服务器首次一次，之后 pull 增量，不覆盖本地已提交内容），grep `INDEX.md` 中与任务相关关键词（症状/报错原文/技术栈/工具名）——命中先读条目再动手。**若正在解的坑库里已有记录而自己没查到 = 检索词没对上，把本次搜索词补进该条目 `keywords`**。
- **解决问题后（强制）**：新坑/新模式按 `~/kb/pitfalls/_template.md` 写入（含证据，人类结论标 unverified）并更新 `INDEX.md`，commit + push；已存在则**只更新 `projects` 指针**，禁止新建重复条目。
- **边界**：项目特有细节留在本项目 `docs/`（P3 单一真相源），KB 只存跨项目泛化条目 + 指回本项目的指针（`项目名: 文档路径#锚点`）。
