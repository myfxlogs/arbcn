# arbcn — Claude Code 入口

> 唯一真相源是 `AGENTS.md`。本文件只是入口壳。

@AGENTS.md

> 兜底：若 AGENTS.md 未自动展开，立即用 Read 读取 `/opt/arbcn/AGENTS.md` —— 它是约束与交接协议的唯一真相源。
> 开工按 AGENTS.md §2 boot；收工按 §6 check-out（含私有记忆清扫 + dialogue.md 落档）。
