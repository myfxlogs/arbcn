# 共享实践记忆

> 高频打回模式 + 代码风格约定。施工 agent 写代码前读，避免被打回。
> 超 ~450 行时最旧内容机械移 LOG.md（留索引行）。

## 默认风格（起步）

- 输出纪律：汇报给结论 + 依据，不堆文件转储。
- 先调研后动手：写前读相关代码、查 git 历史、理解现状。
- Cross-scope：一个 task 只改一个语义范围，跨范围拆成多个 task。
- 不因困难妥协最优解：遇到阻碍回根因，禁止快捷方式（回退代替重构 / 沉默代替修复）。
- 精度 / 并发 / 规模等硬约束见 AGENTS.md §7 门禁 + 后续 constraints.md。

## 已知打回模式（持续追加）

1. **优化/审计提案必须先数据核实再落盘**：2026-08-15 自审计中，北交所打新（需冻结 500 万+）与 QDII-LOF 溢价（限购 10–100 元/日 + 退市新规）两项"更优解"提案被当场证伪。凭印象提"更好方案"而不核实当前市场结构 = 打回。

2. **外部 AI/agent 方案必须逐条核实再采纳**：2026-08-15 千问方案 7 项主张中 4 项有错（工行利率 0.8% 实为 2.8%、两得宝 R5/C5 门槛遗漏、降息前提与加息预期矛盾、压力测试年息套 6 个月夸大 2×）。核实后吸收其 1 个真知（华夏全对冲互认基金）并落 D-018。外部方案的定位 = 候选输入，不是决策；产品准入、风险画像、宏观前提、数学三样必须验。

3. **time.Time 比较一律用 .Equal，不用 `==`/`!=`**：2026-08-15 M2-a 修掉 pgstore/dashboard_test.go 既有坑——arbcn-postgres 服务器 TZ=+0800，pgx v5 读回 timestamptz 带本地时区，`alerts[1].Ts != ts1`（ts1 为 UTC）恒真误报。`.Equal` 只比瞬间（时区无关），`==` 还比 location 指针。同理：写断言前先看读回路径是否带时区。

4. **展示层值格式化必须按 unit 感知，禁止对 kind 一刀切 pct()**：2026-08-15 M2-b 复审（负责人 F2）——FactsSnapshot 对所有 kind 统一 pct()，导致 fx（unit=price，6.7443）显示 "674.43%"、calendar（unit=days，16）显示 "16.00%"。事实值语义由 unit 承载（pct_annualized/pct/days/price/ratio），格式化按 unit 分支；新增事实类型时先看 unit 再决定显示。

5. **快照/投影类 RPC 要排除内部遥测事实**：2026-08-15 M2-b 复审（负责人 F1）——ListFacts 快照投影把 heartbeat（内部遥测，非市场事实）一起返回，与 exporter skipKinds 排除不一致，污染"事实快照"视图。exporter（写 facts.md）和 dashboard 投影（机器可读面）必须同口径排除 heartbeat；实现两处时互相引用锚点，别各写各的。

6. **结算/折算类数值先统一刻度（点数 vs 分数）**：2026-08-15 追溯深审（R6#1）+ M3-a 复审（H1）——rmb 折算与 funding 结算同一根因栽两次。事实层 `pct_annualized` = 百分点点数（6.0 = 6%）；凡是要**乘名义（货币单位）**的，必须先 ÷100 转分数费率。H1 具体：`Per8hRate(10.95)` 原返回 0.01 实为 0.0001，`SettleFundingPnl(0.01, 10000)` = 100 实为 1，模拟 PnL 虚高 100 倍；`RMBDayEnd` 同缺 ÷100。**对抗测试锚点必须锚"正确数值"**——H1 的旧锚点锚了错误值 100，正是靠人工复核才发现，锚点自身的正确性要独立验证。

7. **数值门禁必须防 NaN/±Inf 绕过**：2026-08-15 M3-a 复审（M3）——Go 里 `NaN < x` / `NaN > x` 恒 false，NaN 输入会静默穿过全部 `<`/`>` 门禁，且 NaN 落库污染聚合（SUM→NaN → DAILY_OVER 永久失效）。门禁函数开头对每个数值输入 `IsNaN/IsInf` 检查 → 拒单；配对抗测试（NaN 输入断言拒单）。这条对任何"阈值/超限"逻辑通用，不限于模拟盘。

8. **"置状态 + 建从属行"必须原子，否则不可自愈**：2026-08-15 M3-a 复审（M1）——ConfirmAndFill 先置订单 filled 再逐条插腿，第二腿失败留下"filled 但缺腿"半对冲（违反不赌 D-019），且订单已是 filled、重试被状态守卫拒绝 = 死状态。解法：store 层单事务方法（`FillSimOrder`：UPDATE status 带 `WHERE status='confirmed'` 的 RowsAffected 守卫 + INSERT 全腿），业务代码只调一个原子入口。副作用：状态守卫顺带消掉并发双插。

9. **"非白名单/信任边界"的布尔标记不算可验证门禁**：2026-08-15 M3-a 复审（M2）——`Signal.CarryWhite bool` 由调用方置位，纯函数只查布尔 = 信任边界，不是门禁；数据源/驱动误置即形同虚设。此类信任边界要在 spec 明示，且要在外部数据源接入前落显式配置（如 M3-b 白名单 symbol 集合）。

10. **测试注入时钟要覆盖整个 RPC 路径，grep 掉所有 time.Now()**：2026-08-15 追溯深审（R4#7）——sourceHealth 判定函数接受注入 now，但 ListSourceHealth 入口自己 `time.Now()`，测试注入的固定 now 与运行时 time.Now() 漂移毫秒级 → 边界断言 flake。注入 `svc.Now` 后要 grep 该路径所有 `time.Now()`，入口层最容易漏。

11. **统计性结论只能来自有统计效力的数据**：2026-08-15 M3 文档审计（D-036）——spec 把"funding 套利是否真收敛/收敛速度/残差"列为 M3 主要交付物，但验证载体是 testnet 前向周级小样本（样本量不足 + testnet 费率偏差污染），统计问题用了没统计效力的工具；且 spec 同段"❌ 回测引擎/历史数据回放"与"收敛结论是主要交付物"自相矛盾。裁定：**前向模拟只验证机制（结算管线/行为观察），统计结论由历史数据出**（回填 funding/价差历史 + 收敛分析）。验证目标与方法不匹配 = 交付物承诺它给不了的证据；规格里"不做 X"与"交付物依赖 X"必须互检。

12. **数据源端点假设必须部署机实测，不能信文档/记忆**：2026-08-15 M3-b S4 部署验证——D-031 假定 data-api.binance.vision 镜像 fapi fundingRate，实测 404（该域只镜像现货 /api/v3，/fapi/* 全 404）；OKX 记成 funding-history，实测业务 404，正确端点是 funding-rate-history；数据源 451 封锁也是间歇性（M1-c 通/M1-d 451/M3-b 部署机直连通）。**每次换新数据源/新端点：部署机上用 curl 实测真实响应 + 深度**（OKX funding 历史仅 ~90d，写 365d 窗口必须知道是部分覆盖）再写进代码，写死"data 可用 + 深度够"假设 = 给部署埋雷。与 D-028 先核实再采纳同源，区别在它是针对"端点存在性/数据深度"这类运行期才暴露的假设。**同类还含请求协议格式**：2026-08-15 S3 探针把 Binance 的 Unix 毫秒 timestamp 惯例照搬到 OKX——OKX 要求 `OK-ACCESS-TIMESTAMP` 为 **ISO 8601 UTC**（Unix 毫秒 → 50102 "Timestamp request expired"，实测 3 轮才暴露），且 "一种 API 的签名/格式惯例可移植到另一种 API" 是隐蔽假设（probe_test 已加 ISO 格式对抗锚点）。**WebSocket 端点同样适用**：2026-08-16 D-056 Part B 实测 binance 合约 WS `wss://fstream.binance.com/stream` 在部署机 dial i/o timeout（geo-block，与 REST fapi.binance.com→fapi.binancefuture.com 同源），`wss://fstream.binancefuture.com/stream` 直连通（同数据同帧形）——WS 端点也必须部署机实测，文档/记忆里 .com 往往先入为主，保留 .com 注释防误改回。

13. **门禁/折算的数据面必须按实体类型分派，不能硬编码单一实体的数据源**：2026-08-15 M3-c 复审（D-039）——SPREAD_DRIFT 二次门禁数据面按 spec §10.3 硬编码 `LatestFacts(ticker)` + `LatestFacts(funding)` 双查（这是 funding_hedge 语义），导致 repo（无 ticker，面值锚 100）、carry（无 funding，稳定币生息）两类订单**确认恒拒**——门禁不是"从严"，是功能残缺。修法：按 kind 分派**权威数据源**（repo→reverse_repo 利率、carry→defi_rate 年化、funding_hedge→ticker/funding），fail-closed 语义保持（每类权威源查不到仍拒）。模式：任何"阈值/门禁/折算"若服务多类实体，先枚举**每类实体的数据源**再写数据面；"一类实体的数据面天然通用" = 隐蔽的假设坑。与 #11 同源（验证目标与方法匹配），区别在 #11 是统计工具，本条是数据面映射。

14. **跨数据源同名数值字段口径不同，必须按 source/来源标注口径，不假设语义一致**：2026-08-16 D-040（SimExec 测试网账户区）——`equity_usd` 同名，OKX 给 `totalEq`（交易所精确折算），binance `fapi/v2/balance` 不给 USD 值，只能做稳定币（USDT/USDC/BUSD/FDUSD）合计**近似**（非稳定币无行情折算）。把两路都当"账户权益"展示 = 误导（binance 会低估）。落地：store 类型注释标口径 + 前端按 source 明示「精确 / 近似·非全量净值」+ 无折算资产标 —。任何聚合展示只要跨数据源，先问"这两个同名数字的语义/精确度是否相同"再决定是否并排。

15. **SQL 动态 WHERE 拼接，每个子句必须加括号**：2026-08-16 D-042 演练单拒单根因——`where := []string{"$1 = '' OR kind = $1", "$2 = '' OR venue = $2", "$3 = '' OR symbol = $3"}` 用 `AND` join 但各子句缺括号 → 求值 `$1='' OR kind=$1 AND $2='' OR ...`，`AND` 优先级高于 `OR` → 多参数组合退化为「只生效符号条件」：`LatestFacts(ticker, okx, BTC)` 返回 funding+iv+ticker 五行、首行 funding@binance 负值 → `fundingHedgeSignal` 取 `fs[0]` 拿负值当 ticker 价 → UNHEDGED 拒单。修法：每子句自括 `($1='' OR kind=$1)`。教训：**任何 `strings.Join(cond, " AND ")` 的动态 WHERE，条件内一旦含 OR 必须子句自括**；且测试必须覆盖多参数组合（仅空参/单 kind 测不出）。对抗锚点：TestLatestFactsFilters（删括号必红）。

16. **/proc/PID/exe 与磁盘文件对比必须用 `stat -L`（跟随符号链接）**：2026-08-16 D-042 排查曾误判「运行旧二进制」——`stat -c '%i' /proc/PID/exe`（不带 `-L`）返回的是 procfs 为魔法符号链接分配的**伪 inode**，与磁盘文件 inode 必然不同，被误读为"运行进程加载了已替换的旧二进制"，一度把调查引向反汇编/二进制比对错误方向。真相：`/proc/PID/exe` 是符号链接，跟到目标才是文件真身——**对比运行二进制与磁盘须 `stat -L`，或 `readlink /proc/PID/exe` 看是否 `(deleted)`**。教训：procfs 魔法文件（exe/maps/fd）默认 stat 语义特殊，涉及"运行的是什么文件"一律先跟符号链接再下结论。

17. **"二次点击确认"防误点按钮：`disabled` 不能绑 `pending` 态**：2026-08-16 SimExec 确认按钮 bug——首次点击 `setPending(id)` 进入待确认态，按钮文字变"再次点击确认？"，但同时 `disabled={pending === o.id}` 变 true → 按钮被禁用 → **第二次点击永远触发不了 `onClick`**，确认动作悬死（用户"点击无响应"）。修法：`disabled` 绑 `busy`（确认请求进行中防连点），`pending` 态保持可点（正是等第二次点击）。模式：任何"状态 A → 提示再点 → 状态 B"的两段式交互，第一段的显式状态**不能同时是禁用条件**；禁用只绑"正在处理"（busy/pending 请求），不绑"等待用户下一次输入"。

18. **前端展示枚举值（risk_flags 等）必须对照后端常量全集逐一映射，别只映射见过的**：2026-08-16 SimExec 三处修复——`riskLabel()` 只映射了 2/7 个 Risk 常量（SPREAD_DRIFT/WHITELIST），UNHEDGED 等显示英文原文（业主反馈"英文硬编码"）。后端 `internal/sim/order.go` 有 7 个 Risk* 常量，前端映射表漏了 5 个——**枚举值从后端新增到前端展示，缺联动就是无声回退原文**。修法：映射表逐条对照后端常量全集（UNHEDGED→未对冲 / SPREAD_LOW→价差过低 / SIZE_OVER→单笔超限 / DAILY_OVER→日额超限 / INVALID_INPUT→输入无效），未知名回退原名（演进预留）。模式：任何「后端/DB 枚举值 → 前端文案」的映射表，改后端枚举时必须同步核对前端映射全集（可 grep 断言两端常量集一致）；只映射"见过的值"= 隐藏缺口。与 #14 同源（数据面与展示面口径对齐），区别在 #14 是跨源数值语义、本条是枚举文案全集。

19. **交付自审（§7.3 A–F）之后仍有必须操作的事项：部署应用 + 推送远端，不能停在「自审通过 + 本地 commit」**：2026-08-16 确认下单面板重构 + 三处修复收尾——完成自审、STATE/dialogue/practices 落档、本地 commit 后，把「部署应用（构建 → 重启服务让新 bundle 上线）」和「推送 GitHub」当成可选项晾着，等业主主动说"提交，推送，应用"才补上。§7.3 自审只覆盖代码/文档质量，**不是交付终点**；涉及运行态的改动，业主期望的闭环是「改完 → 构建 → 部署应用 → 推送远端」，中间缺一步 = 交付未完成。修法：check-out 收尾清单显式列入这三步（构建/部署/推送），本地 commit 不代表上线；与"应用面验证完成"分别检查。模式：**自审 → 构建 → 部署应用 → 推送** 是完整交付链；"我审过了、提交了" ≠ "交付了"——运行态改动缺部署/推送，等于给业主留了个没上线的半成品。

20. **洞察/建议类引擎 = 只读证据表面，永不自动改规则**：2026-08-16 D-044 进化建议引擎 L0——业主想要"更智能化"，第一反应是"系统帮我调阈值/自执行"，但规则引擎是人工决策的落点（D-019 不赌 / D-043 不接 LLM），任何自动改规则 = 把决策权交给代码 = 违背项目定位。落地：引擎产出「待核实证据候选」（reject_dist/defi_anomaly/no_order/source_down），每条 insight 的 `actions` 一律写成「核实/评估 → 走 D# 人工决策」，**不落任何自动执行路径**；异常检测用截面中位数×因子（regime shift 稳健），纯函数 + 对抗测试。模式：**任何"系统自我优化/自我进化"的提案，先守住边界——优化方向由数据提示、优化动作由决策层拍板**；"证据面"与"执行面"分离，"系统主动出结论"与"系统自动改规则"是两回事，前者是增强、后者是越权。

21. **施工前必须先对设计文档做 A–F 自审（自行施工/分发施工都须）**：2026-08-16 D-045 计划退出前被业主两次打回——"自我审计没有？""不是给我看，是你应该做的流程，设计文档完成，自行施工/分发施工前都必须自我审计文档。避免完工后审计出错误，又重来。" 教训：第一次只把审计当"给业主看的东西"（计划通过后再补），实为流程缺失——完工后审计出错再返工 = 双重代价（返工 + 信任损耗）。落地：**任何非平凡变更，设计文档完成后先走 AGENTS.md §7.3 A–F 自审，结论逐条写进计划文件，再 ExitPlanMode / 分发施工**；§7.3 已加「两时点强制」条款。模式：**自审是施工前置门禁，不是交付材料**；"我审过了"不该由业主追问才做，且施工前 vs 交付前各做一次。

22. **结算/折算数据面必须按实体 kind 分派；落单 venue/symbol 取事实真实值，勿硬编码**：2026-08-16 D-045 把 carry+repo 接入模拟盘，定位两个潜伏缺口——① `settleOnce` 只查 `fact.KindFunding`（funding_hedge 语义）→ carry/repo 腿"建了仓也永不生息"（D-039 修了确认二次门禁侧，结算侧还漏着）；② repoSignal 落单硬编码 `Symbol:"GC001"/Venue:"domestic"`，而 reverse_repo 事实真实存 `venue=sina/symbol=GC001` → 结算按 (venue,symbol) 查 reverse_repo 永 miss。修法：`settleFactKind(kind)` 单点分派（funding_hedge→funding / carry_asset→defi_rate / repo→reverse_repo）+ `SettleFunding` 带 kind 维（同 (venue,symbol) 不同 kind 互不污染）+ 落单 venue/symbol 用 `fs[0].*` 事实真实值。模式：**「一类实体的数据面/门禁天然通用」= 隐蔽假设坑（practices #13 同源）**——凡服务多类实体的结算/折算/门禁，先枚举每类实体权威数据源再写数据面；且**落单键（venue/symbol）必须来自权威事实**，硬编码 = 下游按真实键查询永 miss。

23. **「算账」与「匹配」是公式不是聪明——写死进系统；经验吸收=人工 D# 落盘、匹配=纯函数、呈现=只读，永不自动吸收**：2026-08-16 D-046 机会实算卡 + 市场结构经验库——业主问「投运后不带你，系统如何自己算账」，直觉是"接入 LLM/更聪明"，但那段算账（瞬时 vs 均值 / 摩擦摊薄保本天数 / 扣摩擦净年化 = 尖峰陷阱）全是确定性公式；业主进一步要「新情况吸收成经验资产自我进化」。落地：**实算卡 = 纯函数写死**（公式 + 三档判定 + 中文模板叙述，D-043 不接 LLM）；**经验库 = 签名纯函数匹配 + 只读呈现**（knowledge_match insight + 浏览 RPC），**吸收 = 人工 D# 落盘（seed 进 git）、验证 = 后续重匹配 + 人工复核 → 新 D# 更新 verdict**，系统永不自动吸收/自动改规则。模式：**任何"让系统更聪明/自我进化"的需求，先拆成「公式（写死）」+「经验（人工吸收、机械匹配、只读呈现）」两半**——公式可对抗测试，经验吸收保决策权（AGENTS.md §0 Claude 最高），系统主动匹配与呈现 = 增强，系统自动吸收改判 = 越权（practices #20 同源）。**NaN 值不可用 0 占位**：NetAnnualized 均值样本不足须显式 `math.NaN()`（0 会被 ratingFor 误读为「净 ≤0 坑」判错档，D-046 实战教训）。

24. **数据 hook 必须随视图生命周期挂载，不能绑 App 根；「刷新」必须覆盖它能刷的全部数据；缺失信号用 presence flag 别用 0 占位**：2026-08-16 D-047 前端第一性原则审计——数据 hook（useSnapshot/useFactsSnapshot/useSim/useKnowledge）绑 App 根生命周期而非视图生命周期，产生三症状：① 非当前 tab 的数据仍 60s 轮询（跨 tab 空转，账本/事实页不可见时也白拉）；② 顶部「刷新」只 reload useSnapshot，同页 ConfirmPanel/KnowledgeBoard 不动（刷新名不副实）；③ useSim 无轮询，8h 结算新单在确认面板是旧数据（新鲜度脱节）。修法：**数据 hook 下沉到展示它的页面组件**（hook 生命周期 = 视图生命周期，切 tab 即卸载、effect cleanup 清 interval），App 根只留驱动全局 chrome 的 hook；页面间需要联动刷新 → 顶部 `refreshKey` 状态递增 + 页面内 hook 依赖它重载（全局刷新 = 根 hook reload + refreshKey 递增）。**0 占位二犯（F4）**：`pnl_rmb===0` 曾被当「汇率缺失」信号（0-as-missing 反模式，practices #7 对 NaN、#23 对 NetAnnualized 同族）→ 真零 PnL 被误标「USD 原值」。修法：响应级 presence flag（`ListSimPositionsResponse.fx_available`，镜像 ListFactsResponse 既有模式），缺失信号用 flag 不用 0 占位。模式：**写数据获取层前先问挂载生命周期——App 根常驻 hook 只留给全局 chrome；页面数据必须随视图挂载/卸载**；「刷新按钮」的覆盖范围必须穷举它该刷的全部数据源（refreshKey 信号是机械可查的联动机制）；任何「0/空/缺省 = 缺失」的推断，换成显式 presence flag（后端响应里加 bool，别在前端猜）。

25. **页面布局由「用户第一眼最需要看到什么」决定，不按组件写码顺序堆叠；裁决先于数据、低频内容折叠**：2026-08-16 D-048 总览页布局重构——总览页原顺序 = 机会数据矩阵 → 告警流 → 确认下单（右列底部）→ 触发器 → 建议 → 经验库，是「按组件功能排」而非「按用户第一眼问题排」。业主指示 UI 第一性原则：**第一眼问题层级决定布局**。对本系统（唯一运营者），第一眼问题降序 = ① 该我行动（待确认订单）② 机会裁决（可抓/打平/坑）③ 系统健康（header）④ 信号流（告警/触发器/建议）⑤ 数据下钻（矩阵）。修法：① 确认下单置顶整宽（该我行动永远第一眼可见，空态保底不消失——保住错误横幅/刷新出口）；② 机会实算卡移面板顶部（裁决先于矩阵），数据块包 Collapse（funding 主矩阵默认展开、低频面折叠）；③ 低频参考面（经验库/全量规则表）折叠，命中（knowledge_match）才默认展开。折叠用原生 `<details>` + 受控 open（Collapse 组件三处复用，零 JS 依赖、无障碍、可 grep），**比自建 state 少 20 行**。模式：**写页面布局前先列「该页用户的第一眼问题」并按重要性排序，再定组件顺序**；「行动/裁决/信号」类必须压在「数据下钻/参考」类之前；低频参考面折叠不丢数据面（均可展开）；布局重构若只是顺序/显隐，纯前端零后端改动（含执行门禁——U 项全程不碰 D-016/MinSpread/白名单）。

26. **布局第 2 课：信息架构（眼睛扫描起点）与操作热区（鼠标顺手高度）是布局的两个独立输入；告警流的第一眼形态是「状态」不是「内容」**：2026-08-16 D-050 总览页布局第 2 版——业主不满意 D-048 直接给精确网格：左上1 数据矩阵（**眼睛扫描起点**，监控数据置顶）、右上1 经验库、右2 确认下单（**鼠标顺手高度 = 操作热区**）、左2 实算卡、左3 建议、右3 触发器；并反转了 D-048「裁决先于数据/确认置顶」子决策（走 D# 留痕）。**告警流第1性原则**：告警 = 系统「有情况报告」，运营者第一眼问题是「有没有 N 条未处置」——**状态问题**，不是逐条内容。故第一眼形态 = 未读数徽标（header 铃铛），时间线 + ack 是拉取式第二需求；网格里整墙告警时间线 = 用内容形态回答状态问题，是反第一性的。模式：① **布局输入分两类**——信息架构（第一眼问题层级，practices #25）与操作 ergonomics（鼠标顺手高度/扫视路径），两者可能冲突，业主拍板优先；**布局迭代业主可反转上一版子决策，必须走 D# 留痕**（决策在环）；② **任何「通知/告警/提示流」先问：第一眼需要状态还是内容？** 状态（有 N 条/未处置）→ 徽标放全局 chrome，内容（时间线/详情）→ 拉取式面板；把内容堆首页 = 反第一性；③ 网格布局用 CSS grid auto-flow（组件顺序即视觉顺序），删组件/换位时顺手删死 CSS（.timeline/.row-col 类案例）与死 props，别留孤儿样式。

27. **CSS grid 的 `1fr` 隐式最小 = `auto` 会撑破视口；媒体查询里的规则特异性必须 ≥ 要压过的基础规则；移动布局必须用真实视口实测（375px）替代推断**：2026-08-16 D-051 移动自适应实测抓到两个真 bug（若只靠视觉/推断看不到）——
① **`grid-template-columns: 1fr` 隐式最小是 `auto`**（等价 `minmax(auto,1fr)`），表格 nowrap 内容（矩阵 min-content 556px）会把轨道撑宽 → 整个网格超视口（实测 375px 视口 docScrollWidth 594）。桌面版用了 `minmax(0,3fr) minmax(0,2fr)` 故无恙；移动折叠兜底写的是裸 `1fr` → 中招。修法：任何想「让内容收缩进容器、超宽走内部滚动」的网格轨道，**必须 `minmax(0,1fr)`**（min 0 才允许轨道小于内容 min-content），`.table-scroll{overflow-x:auto}` 才有意义。
② **媒体查询里的覆盖规则特异性不够时静默失效**：基础 `.ledger-form .ledger-note{grid-column:span 2}` 特异性 (0,2,0)，480px 媒体写 `.ledger-note{grid-column:1 / -1}` (0,1,0)——即使媒体在后、matchMedia 返回 true，特异性先决，note 保持 span 2，在单栏网格上撑出隐式第二列（258+218px，实测 docScrollWidth 511）。修法：媒体块里覆盖同一属性用**与基础规则相同的选择器/特异性**（`.ledger-form .ledger-note`），或直接 `!important`（少用）。
模式：① **grid 轨道只要涉及"可能被表格/长内容撑宽"一律 `minmax(0,1fr)`**（grid 的 min 0 语义与 table 的 auto 行为是常见坑）；② **媒体查询覆盖基础规则时，特异性必须 ≥ 基础规则**——浏览器 cascade 先比特异性再比顺序，媒体在后不等于会赢；③ **移动适配验证 = 真实视口实测**（无头 chromium CDP 取 `docScrollWidth` vs `innerWidth`、`getComputedStyle`、触摸目标 bounding rect），不要只截图/目测——375px 实测把两个"看起来对、实际溢 200px"的 bug 抓出来了；④ 断点对齐既有（600px 主断点与铃铛全屏一致），桌面断点做回归（1280px 双栏 + 按钮不被移动规则污染）。

28. **长列表卡片必须高度封顶（卡内滚动）防无限延伸；DOM 序 = 布局序，重排即提权；业主的布局观察常在移动单列视图**：2026-08-16 D-052 业主五连问（经验库待复核是否自动 / 条目分页还是无限 / 确认下单提上去 / 机会面板分页否则无限拉伸 / 全局阅读协调）——核实 + 三个落地：
① **长列表统一高度封顶**：`.scroll-cap { max-height:min(68vh,520px); overflow-y:auto; -webkit-overflow-scrolling:touch }`（600px 内降 min(52vh,360px)）接入机会面板/经验库/进化建议三处列表——**「卡内滚动」代替「分页」**：实时监控面板卡数 <20，分页引入翻页态 + 丢失跨页一览，滚动封顶以更少交互达成同一目标（不无限延伸）；**翻页态只当条目数预期到百级才值得做**（当前无此预期）。实测机会面板 3092px → 618px、整页 4781 → 2305px。**增长护栏**：未来可能变长的表（defi 矩阵加币种）先给 cap 规则（当前不生效 = 护栏非死代码，注明意图）。
② **确认下单提上去 = DOM 重排**：3×2 grid auto-flow 的视觉序 = DOM 序，把 ConfirmPanel 从第 4 位移到第 2 位（紧跟矩阵）→ 桌面立即成为右上1（首行），移动单列成为第 2 位。**重排即提权，无需 CSS 定位**——grid auto-flow 下想动某卡位置先改 DOM 序，别加 order/定位 hack。
③ **业主观察常在移动单列序**：业主「机会面板在市场结构库底部、确认下单被顶到底」在桌面 3×2 网格不成立（机会=左2 与经验库同排、确认=右2 y875），**在移动单列序完全成立**（矩阵→经验库→机会→确认下单，机会 3092px 把确认下单顶到底）——回用户问题前先想「TA 在哪个视图看到的现象」，再用实测数据（CDP 375/1280）验证。
模式：① 任何可增长列表先问「会不会无限延伸拖长卡片」，是 → 高度封顶卡内滚动（60-70vh 内），并注意移动降级；② **grid/flex 想重排 = 改 DOM 序，让布局序 = 组件序**（唯一真相源，P3），别用散落的 order/position 覆盖；③ **「已核实永不自动」类问题先读存储层代码给证据**（Upsert 不写 validated_at + ON CONFLICT DO NOTHING = 无自动复核路径），再答 UI 层——数据层证据 > 界面推断；④ 布局改动的验证看**每一排左右两卡是否对齐、总页高是否被长卡撑爆**，别只看单卡。

29. **「两卡等高」用网格默认 stretch + 单 cell 包装，别用 per-card 高度硬编码；「复核/确认」类两段式动作 = 人工在环，状态枚举与自由文本判定绝不分家错位**：2026-08-16 D-053 + D-054——
① **等高 = grid 默认行为**：业主要求右1 两卡（经验库上 + 确认下单下）合高 = 矩阵高、进化建议 = 机会面板高。做法：把两卡包进 `.right-stack { display:flex; flex-direction:column; min-height:0 }` 成为**一个网格 cell**，并删掉 D-050 为对齐加的 `.grid { align-items: start }`——grid 默认 `align-items: stretch` 让同排单元格沿行高拉伸，right-stack 自动 = 矩阵高、进化建议自动 = 机会面板高。**零逐卡高度规则**；`min-height:0` 保证堆叠卡内的 scroll-cap 列表可收缩而不撑破等高。若用 per-card `height: <px>` 硬编码，新增内容即破坏等高，且媒体查询下全部失效。**模式：多卡等高的答案先在网格/flex 的默认拉伸机制里找，别去逐卡写数字**；堆叠列容器记得 `min-height:0`。
② **「复核/确认」= 人工在环（HITL），系统永不自动复核**：经验库「待复核」此前全库无写 `validated_at` 的路径 = 闭环断裂；补上 = 新 RPC `ReviewKnowledgeEntry`（signature 必填 + status 三态白名单 + verdict 可留空保留原判定 + validation_note 必填留痕）。**语义关键**：`verdict` 是自由文本判定（「坑」）、`status` 是生命周期枚举（active/superseded/retracted）——初稿曾把枚举写进 verdict 列，自审时靠重读 proto 字段注释纠正；任何「更新一条记录」的接口，先分清哪些字段是**枚举**（白名单校验）哪些是**自由文本**（留空保留语义）。**模式：读接口字段注释（P3 单一真相源）再写写路径**；「待复核 → 已复核」这类状态流转如果系统内无写路径，先补人工在环入口再谈 UI，别让 UI 显示一个永不可能达成的状态。
模式：① 「等高/等宽」先试容器默认拉伸（grid stretch / flex align-items），失败再加显式值；② 状态枚举与自由文本分开设计，别为了省一个字段把枚举塞进文本列；③ 人工复核/确认类动作把「判定文本可保留」设计成显式分支（COALESCE/留空），少一步保留语义的覆盖 = 数据丢失；④ UI 上「待复核」状态必须存在可达的写路径（RPC + 表单），否则是死状态。

30. **功能「只做一半」补全时先核对对称侧 + buf 多模板项目要用独立模板再生成**：2026-08-16 D-055 模拟盘平仓（业主「只做了开仓，平仓没做，就做了 1/2」）——
① **对称补全 checklist**：先列该功能域的「写路径全表」再动工。模拟盘开仓（ConfirmSimOrder）存在时，对称侧平仓（CloseSimOrder）必然缺失——迁移（status CHECK 无 closed）→ store（无 close 事务）→ RPC → fake 接口 → 前端（无平仓按钮）五层都要补；补全时复用既有口径（浮动公式/即期折算/二次确认 UI 模式/宁缺毋滥 add=0），别为平仓另造一套口径（P3 单一真相源）。**「只做了 1/2」类反馈 = 补对称写路径，不是修 bug**；且整单平 vs 单腿平先想好语义（对冲结构单腿平 = 裸敞口，D-019 不赌）再写 store 守卫。
② **buf 多模板项目（dashboard 用 buf.gen.yaml / sim 域用 buf.gen.sim.yaml）改 sim.proto 后，`buf generate` 裸跑不碰 sim 域**——本次改完 sim.proto 跑 `buf generate` 退出 0 但 connect.go 无 CloseSimOrder（静默错过，直到编译报缺方法才发现）。修法：**改哪个域的 proto 就用哪个模板**（`buf generate --template buf.gen.sim.yaml`），再生成后用 grep 验证新符号确实落入生成物（本次 grep `CloseSimOrder` 于 connect.go 确认再继续）。
模式：① 任何「已有 X 方向、缺对称 Y 方向」的反馈，把 X 的完整写路径（迁移→store→RPC→fake→前端）逐层对照补 Y；② buf 项目有多个生成模板时，先查有哪些模板（ls buf.gen*.yaml），再生成对应域，并以「新符号在生成物里出现」为再生成成功判据。
33. **集成测试会对目标库 TRUNCATE/DROP——`ARBCN_TEST_PG_DSN` 必须指向专用测试库，跑测试前先验 DSN；测试残留会伪造出真实症状**：2026-08-16 D-057 事故——上会话 `go test` 把 `ARBCN_TEST_PG_DSN` 配到了真实库 `arbcn`（应是 `arbcn_test`），pgstore 测试 `resetTables` 的 `TRUNCATE sim_orders/sim_positions/sim_account/sim_cash_flow CASCADE` 直接清掉了真实 drill 单 + 留下测试行 + 一个 `capital=0 / cash=123.45` 的 sim_account，曾把「账户资本卡 0」误判成生产 bug。修法 = `testPool` 安全闸：解析 DSN 的 dbname 不含 `"test"` → `t.Fatalf` 拒绝（P4 可检查性：把「误配就出事故」变成「误配就失败」）；同时暴露并修掉一个真实设计缺口——`applyCashFlow` 兜底 upsert 先以 capital=0 建行后，`InitSimAccount` 的 `ON CONFLICT DO NOTHING` 永不补本金（#32 说幂等，但要分「已入金幂等」和「从未入金必须补正」两种语义：改为 ON CONFLICT DO UPDATE 补正 capital=0 + 仅当无 capital_in 流水才入金）。**模式**：① 跑任何会写/删真实库的测试前，先 `SELECT current_database()` 或验 DSN dbname 确认是专用测试库；集成测试基建（TRUNCATE/DROP/全表 DELETE）必须内置安全闸，禁裸奔；② 排查「生产数据被污染/消失」类问题，第一问「最近有没有 `go test` 打过这个库」——测试残留常伪装成真实 bug（本例 capital=0 卡死、孤儿流水引用不存在的订单）；③ seed/幂等写设计要区分「行存在 = 已初始化」与「行存在但从未入金 = 缺初始化」，后者必须补正，否则兜底建行路径（如 upsert）会把它钉死在坏状态。

31. **Go json 解码字段名大小写不敏感匹配——帧里同形不同型字段必须全部显式声明**：2026-08-16 D-056 Part B 根因——binance miniTicker 帧同时有 `"e"`（事件类型，字符串）与 `"E"`（毫秒时间戳，int64）。若只声明 `EventT int64 json:"E"` 而漏掉 lowercase `"e"`，Go 的 json 解码器按字段名**大小写不敏感**匹配，`"e"` 会被匹配到 `EventT` 并尝试把字符串 `"24hrMiniTicker"` 解成 int64 → 报 `cannot unmarshal string into E of type int64`，**整帧 Unmarshal 失败**，合流 read 循环把它当坏帧 continue 丢掉 = feed 静默无数据。修法 = 显式声明 `EventType string json:"e"`（两个字段都声明）。**通用模式**：解析交易所/API 帧前先把字段名 + 类型全部列出来（大小写、同形异义都要），漏一个同形不同型字段 = 整帧丢弃的静默故障；对抗测试用**真实帧形**（含 envelope + e/E 并存）锚定，删字段必红——只发"简化帧"的测试发现不了这个 bug。
32. **「启动即 seed 幂等写」类基建要接线 main.go，不能当死代码留着**：2026-08-16 D-056 Part A——`InitSimAccount` 实现了但从未被任何调用点调用 = 死代码，模拟账户资本从未 seed（账本永远是空的）。修法 = main.go 启动路径接线（guarded by simOK，fail-fast：seed 失败 = 启动失败，重启可重试），seed 本身**单事务**（原实现 INSERT account + 插流水是池级两步，崩溃留半账——修为一个 tx）+ 幂等（ON CONFLICT DO NOTHING，重启不重置 cash，跨重启资金持久）。**模式**：新增 store 方法后 grep 全仓找调用点，无调用点 = 检查是否该在 main 装配时接线；任何「初始化/seed/迁移」类写操作，多步写入必须单事务，且 ID 固定用 upsert 保证重启幂等；「跨重启持久」语义要写清（重启不重置 = 数据在 DB 里，不是进程内存）。
34. **「第 1 原则是盈利」先于执行机制——testnet 验证不了盈利，其价值是让真金测试不被执行层 bug 污染，可选非必经**：2026-08-16 D-058 实盘推进路线（业主两次纠偏）——业主「如果只是验证执行机制，那大可不必；第 1 原则是盈利，策略不盈利执行机制有什么用？」点破：执行机制是手段，盈利是第一性。三层测试分工 = **策略层**（本地 sim 账本 + 真实行情/真实费率 D-037，行情真成交假）/ **执行层**（testnet，机制真但价格/摩擦假）/ **盈利层**（真金，全真）。推论：① **用 testnet 测盈利意义不大**（funding 假、成交薄、摩擦不真实）——「盈利测试」只能靠本地模拟盘真实费率结算（第一道闸，无需 API）+ 真金冒烟（唯一裁决）；② **testnet 的价值 = 冒烟前假钱预演执行全流程，把执行层 bug 清零，让真金冒烟的每分钱只暴露「盈利」这一个未知数**——可选安心垫，不是必经阶段（执行 bug 学费用小资金有界付清，delta 中性风险封顶冒烟资金内）；③ **「模拟亏损 = 路不通」**——本地模拟盘用真实费率仍亏损 = 策略层理论盈利证伪，止投工程时间，别为一条不赚钱的路接执行（D-019 不赌同源：不赚不做的套利才是不赌）。
模式：① 方案设计先问「这条路径验证的是第一性目标（盈利）还是手段（执行/机制）」，手段类验证可降级/后置，别排到第一性验证前面；② testnet/仿真层永远标清「验证机制不验证盈利」的边界，别让假数据输出误导方向决策；③ 亏损证伪 = 停止投入的信号，不是「再优化执行/接入 API」的借口。
35. **「自动证据 + 人确认」= 复核类 UI 的正确形态——系统算当前数据证据、人工点确认才落判定；证据缺失/故障要降级不编造、且不阻断主浏览**：2026-08-16 D-059 复核自动证据（业主「复核完全对应不上、填不上本次复核依据」）——根因 = 条目 rationale 是历史案例，当前数据无对照。修法 = 系统用已核实探测器重跑当前数据面，产出「当前命中/未命中 + 关键数值 + 阈值」只读证据文本（evidenceSpikeTrap/evidenceDefiSpike/evidenceCrossVenue 纯函数，复用 D-046 探测器与数据面口径 P3 同源），人工在证据基础上确认或修改填结论（note 自动预填证据，确认提交才写 validated_at）。**boundary 三不**：① 系统只呈现证据**永不自动改判定/吸收**（practices #20 同源——「自动验证」不是「自动改判」，验证=算证据，裁决=人在环点击）；② 数据缺失给「无法自动核验」**不编造**；③ 数据面故障降级「自动核验暂不可用」但**不阻断浏览**（浏览是主、证据是辅，辅故障不能砍掉主功能）。**模式**：① 任何「人工填表填不上/对不上」的反馈，先看是不是缺「当前数据对照」这一层——给证据别给教程；② 复核/审批类 UI = 系统尽力算证据 + 人做最终裁决（HITL），证据与判定是两层状态，别让系统把判定写了；③ 辅助数据面（证据/建议）故障必须降级呈现而非让整个 RPC 报错，主数据面（浏览/列表）永远优先可用；④ 证据文本要含阈值与关键数值（"ETH 瞬时 10.95% vs 均值 3.75%（×2.9）"），空泛的「建议复核」等于没给证据。

36. **grid 里没写 `grid-column` 的 flex 行会被 auto-placement 塞进最窄轨道；「画布宽度/列比例」是业主关心的第一类布局旋钮；纯 CSS 改动同样要走 build→deploy→验 served bundle**：2026-08-16 对话 #82（业主「经验库更宽 + 复核确认按钮变形 + 整个底画布更宽」）——
① **auto-placement 陷阱**：复核表单 `.review-form` 是 3 列 grid（`110px 1fr 1fr`），`.review-actions`（取消/确认复核）是 flex 行但**桌面端没写 `grid-column`** → grid auto-placement 把它放进 row 2 的 col 3（正好是最窄的 110px 轨道），两按钮被挤变形；手机端媒体查询里有 `.review-actions{span 2}` 所以手机没事、桌面漏了。修法 = 显式 `grid-column: 1 / -1` + `justify-content: flex-end`。**模式：grid 子项若不想被 auto-placement 任意落位，行级/轨道级都要显式 span——尤其「桌面没写、手机写了」的覆盖最易漏**；发现"按钮/元素被挤变形"先算它落在哪格，再想间距。
② **画布宽度（`main` max-width）与网格列比例（`fr` 值）是业主感受"窄/宽"的第一变量**：1100→1440px + 3fr:2fr→5fr:4fr 一次满足「经验库更宽 + 画布更宽」（右列 ~421→619px、左列 631→773px 同步加宽无回归，860px 折叠单栏不受影响）。**模式：布局"太挤/太窄"类反馈，先动全局画布与列比例这两只大旋钮，别只在某个组件里塞宽度 hack**；改 `fr` 比例要算两侧新宽度确认都变宽（此例右列是诉求、左列不得变窄）。
③ 纯 CSS 改动照样产出新 bundle hash（本次 index-D9dhO0s4→index-Cyt2Tw60.js / index-Dok6zbWG.css），**practices #19 三步闭环同样适用**：npm build → go build 嵌入 → 原子替换重启 → 验 served bundle == dist + healthz。别因"只改了样式"跳过部署验证。
④ **flex 容器里的子元素若自带 margin，会与容器 gap 叠加成"双倍间距"**：本次右列三卡实测间距 31/32px（应为 16）——每卡保留基础 `.card{margin-bottom:16px}`，`.grid > .right-stack{margin-bottom:0}` 只清零了 right-stack 自身的外边距、没覆盖其内部卡，16px margin + 16px flex gap 叠成 32px，把右列撑高到超过左列矩阵、整页被拉长（页高 2055→1999，矩阵 998→941）。修法 = `.right-stack > .card{margin-bottom:0}` 让间距只由 flex gap 承担。**模式：凡「margin + gap 同向」的布局，先算两数叠加；清零外边距要覆盖到容器内部的子元素层级**，CDP 测 `cardGaps`（下一卡 top − 上一卡 bottom）比看样式值更可靠。
⑤ **表单/工具栏行对齐用 `align-items:end` + 显式 `grid-column`，比 auto-placement 靠谱**：复核表单三列改 `110px 1fr auto`，复核结论 span 2 + 按钮 `grid-column:3` 同行、`align-items:end` 底部对齐输入（实测按钮底 == 输入底 0px）；手机 ≤640px 网格变 2 列时基础 `grid-column:3` 会隐式建第三轨道 → 必须媒体覆盖回 `span 2`（整行、按钮落输入下方触屏友好）。**模式：`grid-column` 的列号依赖轨道数，改网格列数/断点时要同步核对每个显式列号的子项**——「桌面 col3、手机 span 2」这类覆盖最容易漏。

37. **新增可空列必须 *string/pgtype 兜底扫描 + 存量 NULL 回归测试（上线即抓的真实事故）；「复核快照 + 翻转比较」= 把判断编码进确定性机制的正解**：2026-08-16 D-060（业主「没有 claude 系统怎么做判断」→ 路线 A 落地，上线当天抓 503）——
① **NULL 扫描事故**：给 knowledge_entries 加可空列 review_direction 后，pgstore `ListKnowledgeEntries` 直接 `&e.ReviewDirection`（string）扫列 → 真库里有复核早于该列的存量行（review_direction = NULL）→ `cannot scan NULL into *string` → **ListKnowledgeEntries 503 → ListInsights 连带 503 → 前端快照 Promise.all 静默失败卡「连接服务中」**。单测全绿（fake 用 Go 零值、永远没有 NULL），浏览器实测先暴露。修法 = `var rd *string` 扫列、nil 检查后赋值。**模式：新增可空列，扫描必须 *string/pgtype 兜底，且必配「存量 NULL 路径」回归测试（插一条不填新列的存量行再 List）**——真库存量 NULL 单测测不出，靠 browser 实测 + 显式存量兼容测试补上。
② **「复核快照 + 翻转比较」= 把判断编码成确定性机制**：业主问「没有 claude 系统怎么判断」——答案是系统不需要「判断」：复核时存「证据方向快照」（hit/miss），之后每次对当前数据重算方向、与快照布尔比较，翻转 = 建议复核、一致 = 仍适用。**方向在证据生成时刻由同一函数产出（evidenceResult{text,hit,known}），不反解析展示文本（P3 单源）**；系统永远只「比较」不「裁决」，改判仍人工 D#（practices #20 边界）。**模式：任何「没人盯着怎么办」的判断需求，先问能不能编码成快照 + 布尔比较**（可判 = 无需 LLM/无需人在环）；「复核早于新列」的存量条目无快照基线 → 诚实标「已复核」而非虚称「仍适用」，三态变四态（待复核/已复核[无快照]/仍适用/建议复核），宁缺毋滥。
模式：① 加可空列必配 *string/pgtype 扫描 + 存量 NULL 回归测试；② 判断需求先试「快照+比较」确定性编码，别急着引 LLM/人；③ 复核类状态区分「无基线」与「一致」，诚实标注，别虚称适用。

38. **「过去历史不代表未来」的认知落实践——回测能证伪不能证真；判定门类测量必须区分「环境有无机会」vs「策略能不能赚」**：2026-08-16 对话 #85（业主「关于回测，我并不大迷信，过去的历史并不代表未来的行情，我认为第 1 性原则是这个策略是否真的能盈利，跑 bot 的前提是策略确定性能盈利才能跑 bot」）→ 落 D-061（D-058 补充条款）——
① **回测能证伪不能证真（不对称）**：证伪可信——历史高费率时段也覆盖不了摩擦 = 结构性否定；证真无预测力——回测赚钱对未来行情无预测力（过拟合 + 行情结构变化 + 摩擦低估）。**模式：交易策略一律不做「收益预测回测」，最多做「门禁条件回放」可选自检**（用策略自己的过滤条件（如 ≥15% 高费率档）回放历史，验证门禁会不会正确触发、扣摩擦是否为正——这是自检「抓窗口机制」不是预测「未来收益」）；证伪结果可当作止投证据，证真结果不可当作可投证据。
② **判定门类测量必须分离「环境有无机会」与「策略能不能赚」**：阶段 0 判定门① 若在**无机会环境**（当前 funding 4–7% vs 15–30% 窗口档不存在）跑 30 天前向 paper，结果大概率 ≈ 零单 ≈ 0 PnL——这测的是**环境**不是**策略**，把「当前无窗口」读成「策略不盈利、止投」= 在没下雨的 30 天否定雨伞设计（宁缺毋滥 D-019：无机会时正确行为就是零单）。**模式：任何「跨窗口收益判定」，测量时记录环境条件（当期 funding 中位数/可交易面/有无窗口档）随结果留档**；无机会环境的零 PnL 标「当前无窗口」而非「策略失败」。
③ 前向验证信号薄时，**摩擦假设与窗口代表性主导结论走向**（诚实基线 3.2–3.7% ≈ 0.3%/月，摩擦 0.3% 一个量级就够翻结论）——摩擦用已核实值（D-046 业主核实普通主户 0.3%），窗口代表性靠环境留档，别用「纸面参数」糊结论。

39. **跨层数值比较必须统一单位（fraction vs percent points）；判定门类测量必须自检数据面可信度（判定门自己也会骗人）**：2026-08-16 D-062/D-063（业主「所有决定引进的方案都要落地」+ 中途补充「判定门① 自己也会骗人，有条件一起解决」）——
① **单位错配是「判定门自己骗人」的真实形态**：TWR/MWR 纯函数返回小数（0.708 = 70.8%，数学惯例），判定门① 阈值（GateThreshold=4.0）与 RPC 字段是**百分点点数**。审计中发现直接拿 0.708 跟 4.0 比 → **恒 FAIL**（无论策略多赚永远判定不过）；反向若拿 4.0 当 0.04 也会恒 PASS。**模式：跨层比较（纯函数 ↔ 判定阈值 ↔ RPC 字段）必须统一单位，换算在编排层（RPC 组装处）一次完成、此后各层共用同口径，并用测试把换算结果钉死**（本测试锁定 TWR/MWR ≈ 70.84，删 ×100 必红）；纯函数保持数学惯例不被迫改语义。
② **判定门的数据面自己会骗人**：快照缺口（服务中断跳过链乘段）、数据损坏（equity 恒等式破坏）会让 TWR 静默失真，但 gate 照判 PASS/FAIL——**测量引擎拿自己采的数据自己出题自己答卷，须自证可信**。**模式：任何「跨窗口收益/绩效判定」先做数据面自检再判**——覆盖率（实际/期望落点，<90% 判定不采信）+ 数据完整性（恒等式 + 时间序）先行；损坏任何时候不采信（映射 DATA_ANOMALY 不编造，practices #7 同源）、窗口未成熟（<30 天）不误判数据坏、部分缺口附加警示但不覆盖判定；判定用「覆盖率/完整性」自证与对抗测试锚定（删写入必红 / 删校验必红 / <90% 不采信必红）。

40. **400+ 行的 hook/组件文件加字段就顶穿 450 check-lines 硬线——触发用纯值/兜底常量拆独立小模块；部署验证升级为 md5 逐字节比对 served bundle vs dist**：2026-08-16 D-064（hooks.ts 463→449 行，check-lines 硬阻断）——
① **顶线教训**：useSnapshot 的 Snapshot 接口加两个字段（window/windowPairs）+ Promise.all 加第 9 路 RPC + 兜底常量 emptyWindowStats，就把 hooks.ts 从 ~430 推到 463 行 > 450 硬阻断。修法 = 触发用的纯值/兜底常量拆到独立小模块 `windowDefaults.ts`（18 行），主文件只留 import——与 D-062 把 PerformanceZone 抽独立组件防 SimExec 超线同模式。**模式：靠近 450 线的文件，加「值/常量」先考虑独立小文件（零逻辑的声明不污染主文件行数），加「组件/区块」先考虑独立组件文件**；check-lines 是硬闸不是建议，提交前必跑（commit 也被 hook 挡）。
② **部署验证升级**：此前验 served bundle == dist 只比文件名 hash（index-X.js == index-X.js），但上次 D-062 部署发现 bin 比 dist 早 28min 导致 396B 内容漂移（hash 名相同、内容不同）。本次改为 **curl 取 served JS/CSS + 本地 dist 文件双方 `md5sum` 逐字节比对**（而不是只看 index.html 引用的名字）——名字对 + 内容也对才算真匹配。**模式：部署验证 served bundle == dist 时，比「文件名」更要紧的是「内容字节」——curl 实际资产算 md5 与磁盘 dist 比对；index.html 引用的 hash 名只证「引用对」，不证「内容新」**。

41. **门禁类机制必须可检查（P4）——休眠也要可见；摩擦口径分用途统一到已核实值；靠近 450 线的文件用「独立组件自包含拉取」**:2026-08-17 D-065 修订（业主纠偏「不做可选，是每个策略都自动做，做成门禁」，对话 #88）——
① **门禁可检查性（P4：无法机械检查的规则必被违反）**：回放门禁在当前数据下大概率 `no_window`（历史无 ≥15% 档，D-061②）——**如果门禁只在拒单时才暴露（负样本），休眠时业主完全看不见它在跑、会怀疑机制不存在**。修法 = 只读证据面 RPC（`GetReplayState`，每策略 kind 当前判据 verdict/note/样本/净年化）+ 前端卡（自包含拉取 + 60s 轮询），**门禁休眠（no_window）也可见 = 证明机制在跑、且输出的是正确的「环境无窗口」而非故障**。**模式：任何「自动执行/自动否决」的机制都要配一个「它现在在想什么」的只读证据面，别让存在性靠负样本或出错才暴露**。
② **摩擦口径分用途统一**：sim report 回填 `defaultFrictionRate=0.002`（0.2%）与 D-046 核实 0.3%（oppcalc 门禁/回放门禁用）并存——**历史回填 PnL 的口径（0.2%）旧于已核实值（0.3%），改它影响历史 PnL 与 D-062 判定门① 基线，须谨慎整体核算，本次不擅动**（D-065 修订 ⑥ reconciliation 记录）。**模式：同一物理量（摩擦）在回填 vs 门禁两处取值不同时，必须显式记录差异与待统一项，别让两个常量默默分叉**。
③ **强制门禁的判据值域要「放行态也自明」**：`no_window`（环境无窗口）与 `pass`（证伪未发生）都放行但语义不同——前者是「没数据/没窗口 = 机制休眠」，后者是「有窗口且净正 = 机制未被否定」；两者都不得与「机制故障/验证失败」混淆（practices #38 证伪不证真：pass 非收益预测）。**模式：放行态要区分类别，自述 note 说明「为什么放行」，别让绿/灰通行证掩盖机制状态**。

42. **T2 时间序列历史写进 T1 文件 = 分层预算违背；同型病（无界增长）跨项目两发——根治 = 封顶 + 节流 + 历史交 git**：2026-08-17 D-066（arbcn facts.md 快照 90 份/3404 行）+ arb D-096（audit.pb 2.57GB）同型根因，arbcn 先治——
① **根因形态**：`internal/exporter` 每次导出（boot + 24h 定时 + 每次规则 armed→active）追加整份新快照、旧快照标「已过期」不删除 → **T2 级时间序列历史（只存不读）写进 T1 事实库**（T1 预算 ≤450 行）；规则触发是体积主力（90 份/约 4 天 ≈ 20 次/天）。**模式：任何「按事件追加、永不删除」的自动写入，先问一句「这是 T2 历史还是 T1 知识」——时间序列历史归 git（P2「历史机械滚出活跃层（git 保留，token 不付）」，facts.md 每次 check-out 提交即归档），堆在活跃文件里是重复付费**。
② **根治三件套（封顶 + 节流 + git 保留）**：**封顶** `maxSnapshots=5`（段内只留最近 N 份，超出从尾部整体移除——有界 = 文件上界常量）；**节流** `ruleTriggerThrottle=10min`（规则触发导出距上次成功导出 < 阈值合并跳过，快照是「最近状态」参考面，DB + RPC 有精确时刻值；节流同时让 N 份窗口跨更长时间，防同一突发写出一串近同快照）；**历史交 git 不另写 LOG.md**（P3：另写一份 = 同一事实两份，机器快照历史 git 即归档，别为「归档感」加冗余机制）。
③ **D# 规则适用范围修订要显式**：D-028「已过期不删除」规则对**手工事实行**（少量、带 D# 锚点、需追溯）保留，对**机器快照段**（bulk 镜像、高度重复）改封顶淘汰——**同一文档里「curated 事实」与「机器镜像」性质不同，一条规则不能一刀切覆盖两种对象；修订时显式写明适用范围边界**。
④ **节头漂移是潜伏结构债**：「## 监控快照」节头在历史演进中被逐次插入的新快照顶到段中部（每次导出 new 插最前，节头被挤下）——**「新块插最前」的追加式写文件，节头/说明块会随插入次数逐次下移漂移，顺手归一重置到段顶，让块布局恒定可机械检查（P4）**；对抗测试删封顶行/删节流判断实测必红，部署后 boot 导出自愈存量（90→5 份）。

43. **单体文件拆分首选「物理分行 + 顺序 import」，零回归锚点 = 产物逐字节一致，别只靠肉测**：2026-08-17 D-067（hooks.ts 450 行卡 check-lines 硬线 + style.css 1248 行无门禁，业主「全拆：hooks + CSS + CI」）——
① **CSS 拆分的零风险法 = 按原顺序物理切分 + 入口按原顺序 import**：style.css 1248 行按注释段边界切成 12 个文件（00-base…11-account-responsive），`main.tsx` 按原顺序 import 一列——**import 顺序即级联顺序**（CSS 靠层叠/覆盖顺序生效，级联由 import 顺序保证）；切分脚本必须验证**拼接无损**（`''.join(parts) == original`，Python `splitlines(keepends=True)` 保行尾），空行边界要算准（cut 边界是空行时该空行必须留在前文件，否则拼接丢行——D-067 实战 AssertionError 抓过一次）。**模式：拆「顺序敏感」的文本（CSS 级联/编译顺序/手写序）时，物理分行 + 保留相对顺序 + 拼接断言 = 零逻辑改的最小方案；产物逐字节 diff（md5）作验收，通过 = 零视觉回归，无需逐视口肉测兜底**。
② **hooks.ts 拆分 = 纯物理搬迁 + barrel 保 import 路径不变**：6 个 hook 零依赖整块搬 hooks/ 目录 + `index.ts` barrel re-export，消费方 `./hooks`/`../hooks` import 路径不变（`moduleResolution: bundler` 自动解析 barrel）——**消费方零改动 = 最低回归面**；tsc `noUnusedLocals` 逼出每文件只 import 自己用到的类型（搬完顺手清理，缺 import 编译必红 = 可检查锚点）。**模式：拆 JS/TS 单体先查依赖图（有没有 hook 间共享状态/循环 import），零依赖 = 整块搬零逻辑改；barrel re-export 保外部路径兼容是标准手法**。
③ **「无门禁的文件」是隐性债**：style.css 1248 行不被 check-lines 覆盖（只查 .go/.ts/.tsx）——**任何不受门禁约束的资产都会缓慢变大，补门禁比祈祷自律可靠**：拆到各 <200 行回归门禁管辖（P4）。

44. **CI 照搬已验证骨架 + 按本地事实裁剪；`go test -race` 是既有测试注入时钟 data race 的照妖镜**：2026-08-17 D-067 补 CI——
① **照搬 arb 已验证骨架 + arbcn 适配**：.github/workflows/ci.yml = checkout@v4 / setup-go@v5 go-version-file / setup-node@v4 node-22 cache:npm / npm ci / npm run build / go mod download / go build / go vet / go test -race / check-lines。arbcn 无 broker/真金/EnsureMigrations，无需裁剪项。**关键顺序约束：前端 build 必须在 go build 之前**（web/embed.go `go:embed all:dist`——顺序反了嵌入空 dist → 404，本地开发同）。**首版不加 govulncheck**（arb 也未在 CI 跑；arbcn go.mod 缺 `toolchain go1.26.6` 钉版，加了会先扫出 stdlib 漏洞需另钉版 = 引入 scope 外工作）；**不加 postgres service**（DB 集成测试无 `ARBCN_TEST_PG_DSN` 自动 skip practices #33 = CI 首跑即绿；network 门禁测试纯文件扫描无外部依赖）。**模式：引别人的 CI 先问「哪些步骤依赖本仓没有的能力」，没有就裁；别把别人为了兼容历史加的步骤原样搬来**。
② **`go test -race` 暴露「测试在 goroutine 执行期间直接写被测对象字段」的 data race**：exporter 测试 `go x.Run(ctx)` 后 `x.Now = func()...` 推进时钟，与 Run goroutine 内 `x.now()` 读并发（本地 `go test` 不带 -race 全绿，CI `-race` 必红——**race 只由 -race 暴露，`go test` 绿不代表无竞态**）。标准修复 = **atomic.Value 时钟闭包**：goroutine 启动前 `var clock atomic.Value; clock.Store(t0); x.Now = func() time.Time { return clock.Load().(time.Time) }`（写一次闭包），之后测试只 `clock.Store(t1)` 推进——读走 Load，无同步开销外的竞态；删修复必红。**模式：被测对象注入的 `Now func()` 若被 goroutine 内读、测试内改，必须用 atomic.Value 闭包（或 channel/互斥）而不是裸变量赋值；跑完 CI 复刻的 `go test -race` 再进 check-out**。

45. **展示层价格格式化按量级自适应小数位，写死小数位会吞掉低价币的真实波动**：2026-08-17 业主报「TRX 的价格一直没有变化？」——SSE 实测 TRX 每秒在跳（0.33196→0.33231），数据面完全正常；根因 = `QuoteStrip.tsx` 用 `toLocaleString("zh-CN", { maximumFractionDigits: 2 })` 写死 2 位小数，TRX ~0.33 的波动发生在小数点后 3~4 位，全被四舍五入成恒定 `0.33`，观感「永不变化」（BTC/ETH 高价位 2 位够、TRX 低价位不够）。修 = `fmtPrice` 按量级自适应：`≥1` 保留 2 位（BTC/ETH 显示不变）、`<1` 保留 4 位（TRX 0.3320→0.3323 跳动可见）。**模式：价格/费率类展示先问「这个标的的量级在哪」再定小数位，按量级分档（1 元以下给 4 位）；「看起来不变」要先查数据面（抓流/查库）排除显示层精度，再怀疑数据源——这次先抓 SSE 实证数据正常，把 bug 锁定在展示层，没冤枉数据面**。

46. **对抗守卫改语义必须同 commit 改测试——「允许做 X」由测试双面钳制，禁主网 token 回流**：2026-08-17 D-098 测试网执行层——`simtestnet/domains_test.go` 的 `TestNoOrderEndpoints`（68-87 行，断言「零下单路径」）是 D-034 ②「testnet key 只读探针」的对抗锚点；本里程碑把 testnet key 从只读探针扩展为「可下单」（仍 SIMULATED 隔离），守卫语义随之改变。修法 = **同一 commit 内改代码 + 改测试**：`TestNoOrderEndpoints` → `TestOnlyTestnetOrderEndpoints`（断言 executor*.go 只含测试网下单端点字面量 `/fapi/v1/order`、`/api/v5/trade/order` 等，union 锚点）+ 新增 `TestOnlyTestnetDomains`（主网域 binance.com/fapi.binance.com/okx.com 禁）+ `TestProbePathsReadOnly` 保留（probe.go 仍只读）+ `TestExecutorPaths` 新（executor.go 仅测试网下单端点）——**双面钳制**：一方面证明「写路径只能打测试网」，另一方面证明「主网域/token 永不回流」，删任一必红。另：simapi 的 `TestNoRealTradeTokens`（禁 account/withdraw/transfer/order 端点/token + 主网域）与 sim 的 `TestNoNetworkImports`（禁 net/http/crypto/tls/crypto/hmac）**不因功能扩展而放宽**——镜像走接口注入（simapi import simtestnet 只取类型、源码文本扫描仍绿），sim 保持零网络零密钥。**模式：① 任何「扩大系统能力」的变更，先盘点哪些对抗守卫在断言「旧限制」，把它当需求修订而非障碍——守卫语义 = 决策的机械编码，改决策必须同步改守卫并同 commit（P4：守卫与决策双写源，留一处旧的就是自相矛盾）；② 守卫改语义后，用「允许面」与「禁止面」两个测试钳住新边界（双面钳制：能打的 vs 绝不能打的都显式断言），别只把旧断言改负样；③ 零网络零密钥类守卫是架构红线，功能扩展时优先走接口注入/依赖注入保持红线不变，而不是放开红线**。
---
