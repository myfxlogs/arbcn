# STATE — 当前工作状态 + 交接负载

> 开工读 / 收工写。滑动窗口（AGENTS.md §4）：只存进行中 + 近期完成 + 交接负载；最旧 ✅ 移 LOG.md。

## 交接负载

- 现状: **D-060 复核方向快照 + 系统自动翻转检测已部署闭环**（对话 #83，业主核心追问「没有 claude 系统怎么做判断」→ 路线 A 决策，不做 B(LLM) 理由已落 decisions D-060）：**复核时存「证据方向快照」**（review_direction hit/miss，`COALESCE(NULLIF(''))` 数据面不可判定 → 保留旧快照不覆盖历史）+ **系统自动翻转检测**（`recheckNeeded` = 当前证据方向 ≠ 复核时快照方向，纯布尔比较只比较不裁决）+ 前端**复核四态**（待复核 / 已复核[无快照基线，诚实不虚称适用] / 仍适用[有快照且一致] / 建议复核[翻转]）+ **待决策层清单**（翻转置顶 + 警示条 + 建议复核按钮，D-052 同源）；**上线当天抓真实事故**：pgstore 扫 review_direction NULL 致 ListInsights 连带 503（存量复核早于该列的行 = NULL）→ `var rd *string` 兜底 + TestReviewDirectionNullScan 存量 NULL 回归（单测全绿因 fake 无 NULL，browser 实测先暴露）；**CDP 实测两态**——正常态（3 条生效中+已复核、无 banner、12px 间距、右列顺序 [经验库,确认下单,平仓] 无溢出）+ 翻转态（banner「建议复核 1 条」+ 资金费率尖峰陷阱置顶 warn 芯片 + 按钮「建议复核」recheck-open + 「上次核验 未命中」）经 **Fetch 拦截注入验证，不污染生产库**（prod SQL UPDATE 被权限分类器拒，未绕过，改拦截验证）；对抗测试 3 组（快照+翻转 / 故障面不覆盖旧快照 / 无快照不误标）+ fake 四处签名对齐；**零执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**；verdict 改判仍人工在环（validated_at 只由人工复核写，practices #20 边界不动）。**D-060 后续：SPREAD_DRIFT 拒单理由自明性改进已部署**（对话 #84，业主查 id=18 拒单发现「预期年化变化 53.28%」被误读为涨到 53.28%、实为回落 53% → confirm.go 两条漂移理由改自明格式：`预期年化 5.80% → 2.71%（回落 53.30%，超阈值 ±20%）` / `参考价 62000 → 65000（漂移 +4.84%，超阈值 ±2%）`，前后数值+方向词+阈值，测试含回落/上行对抗；**id=18 历史 note 经业主确认一次性回填新格式**（`预期年化 5.80% → 2.71%（回落 53.28%，超阈值 ±20%）`，数值沿用当时真值，RPC 验证订单历史已显示；审计留痕例外——业主显式授权、纯展示修正不改逻辑；后续新拒单自动用新格式；其余拒单理由已带数值无需动）。**订单历史表布局修复已部署**（对话 #84 后续②/③，业主「理由不能完整显示 + 左边空白」+「桌面收太紧内容重叠」→ 根因：`.note-cell` 220px nowrap ellipsis 截断（内容 519px 只显 300）+ `table.rows` 全右对齐 + 自动布局列撑宽致列内左侧空白 → 修：作用域限定 `[aria-labelledby="sim-orders-title"]` 的 `table-layout:fixed` + 显式列宽（首版估算过紧致桌面重叠，**按 CDP 实测自然宽重设**：时间165/状态90/类型125/标的135/方向52/数量75/预期68）+ 文字列左对齐/数字列右对齐 + **备注列吸收剩余去截断可换行**（桌面 489px 完整两行显示）+ 类型/标的长资产名断行兜底 + min-width 1010px 防小屏备注压成 0 宽竖条；CDP 复测桌面全列 overflow=false 无重叠、375 无溢出、SimExec 流水表 note-cell 未误伤；**后续④ 表头标题居中 + 单元格值左对齐已部署**（对话 #84 后续④，业主「表头标题应在值单元格中间：表头居中、值左对齐」→ 作用域规则替换为全局 `th{text-align:center}` + `td{text-align:left}` 覆盖全列含数字列——业主明确值左对齐优先于数字列右贴；CDP Range 精确测表头文字中心偏移 0px 与单元格中心完全重合 + 8 列 th center/td left；served bundle == 新 dist（index-BajggGUF.css / index-DcgC6jxF.js）+ healthz ok，375 无溢出 + SimExec 流水表未误伤）。此前：**画布加宽 + 右列间距/顺序 + 复核表单布局已部署应用**（对话 #82，纯前端 CSS + OverviewPage DOM 序）：`main` max-width 1100→1440px（整个"底画布"就是 main）+ `.grid` 3fr:2fr→5fr:4fr（右列经验库/确认下单/平仓 ~421→619px、左列矩阵 631→773px 同步加宽无回归，860px 折叠单栏不受影响）；**右列三卡间距 31/32→12px（根因 = 双倍间距：内部卡自带 .card margin-bottom 16px + flex gap 16px 叠加；`.grid > .right-stack` 清零未覆盖内部卡 → `.right-stack > .card{margin-bottom:0}`）+ gap 16→12px**，页高 2055→1999（矩阵 998→941 回落，右列不再撑高左1）；**确认下单/平仓顺序对调 = [经验库, 确认下单, 平仓]**（行动优先 D-052 延续）；**复核表单按钮与复核结论同行**（`110px 1fr auto`，复核结论 span 2 + 按钮 col3 右对齐底对齐 0px，手机 ≤640px 改回 span 2 整行）；**复核按钮变形根因** = 桌面端无 grid-column 被 auto-placement 塞进 110px 首列。部署实测（CDP 1280/375）：间距 11/12px + 顺序正确 + 页高 1999 + 按钮同行 0px + 375 无溢出；served bundle == 新 dist（index-Cyt2Tw60.js / index-Dok6zbWG.css）+ healthz ok；零 JSX/后端/RPC/门禁改动。**D-059 复核自动证据 + 总览平仓卡 + 订单历史已部署应用**（对话 #80/#81，业主三条 UI/复核需求，AskUserQuestion 确认「自动证据+人确认」与「模拟执行 tab 订单历史」）：**复核自动证据**——proto KnowledgeEntry.current_evidence + dashboard/knowledge.go `knowledgeEvidence`（evidenceSpikeTrap/evidenceDefiSpike/evidenceCrossVenue 三纯函数，复用 D-046 探测器与数据面口径，产出「当前命中/未命中 + 关键数值 + 阈值」）+ `latestByVenueSymbol` 数据准备共享（knowledgeMatches 命中信号与 evidence 复核证据同源，P3 一处两用）+ ListKnowledgeEntries 编排（数据面故障降级「自动核验暂不可用」不阻断浏览 / 缺数据「无法自动核验」不编造）+ 7 测试（3 hit/miss/缺数据/故障降级，fakeStore 加 factsErr 独立故障域）+ 前端 KnowledgeBoard「当前核验」证据行 + ReviewForm 复核结论 note 自动预填（证据开头「自动核验：」即预填，确认或修改后提交 = 人工在环裁决，practices #20 系统永不自动改判定）；**ClosePanel 平仓卡**（市场结构与确认下单之间的空白 → 当前持仓订单级合并一行 + 整单平 armed 二次确认 D-019）+ ConfirmPanel/ClosePanel 列表接 `.scroll-cap`（待确认单多不向下延伸，「确认成交」按钮始终视口内）；**OrderHistoryZone 订单历史**（模拟执行 tab：全量六态 sim_orders + sim.tsx `statusText` 中文徽标 practices #18 + **rejected 拒单负样本保留 note 原因**——live 库实测 id=18 rejected SPREAD_DRIFT 53.28%）。部署实测：全量测试/vet/npm build 绿 + 原子替换重启 healthz ok + served bundle == dist（index-DRVZi1Rk.js 含「订单历史/当前核验/确认平仓/已拒单/自动核验」）+ live ListKnowledgeEntries 三条 evidence 正确（spike_trap 命中 ETH 10.95% vs 均值 3.75%（×2.9）/ cross_venue 命中 BTC 7.5pp·ETH 8.3pp / defi 未命中 SUSDE 4.39%）+ 拒单 18 负样本在库。**零执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动；复核仍人工在环；平仓/确认仍 SIMULATED 人工触发（D-019 不赌）；不接 LLM（D-043）。** 此前：**D-056 模拟账户「按真实账户对待」完整现金账本 + 秒级实时报价已部署闭环**（对话 #73，业主两条需求）：**Part A 现金账本**——migration 0009（sim_account 单账户 id=1 + sim_cash_flow 逐笔流水）+ store 接口（SimAccount/CashFlow/4 kind 常量 + SimLegClose.CashDelta + SettleSimPosition→SettleSimPositionFunding + InitSimAccount/GetSimAccount/ListCashFlows）+ pgstore（applyCashFlow/applyOpenCashFlows 公共 helper，open/funding/close/capital_in 四事件全在既有事务内原子入账，Fill/Accept 两成交路径口径一致）+ main.go 启动 seed InitSimAccount(simCfg.Capital)（幂等重启不重置）+ GetSimAccount RPC（equity=cash+market_value=capital+realized+unrealized 双恒等式交叉校验 + 最近 100 条流水）+ CloseSimOrder 服务端补 CashDelta + 前端 AccountZone（五数对账网格 + 净值 USD/RMB + 逐笔流水表，账户区置顶第一眼）；**Part B 秒级实时报价**——上游 WebSocket（binance USDT-M 合流 miniTicker + okx v5 tickers 公共行情，无密钥 D-010）/ 下游 SSE（新包 internal/quote，断线指数退避自愈 + wsDial 8s 拨号上限 + 45s 读空闲 + /quote/stream 1s 差分推送）+ gorilla/websocket 唯一新依赖 + 前端 QuoteStrip 顶部报价条（EventSource 自动重连，binance/okx 双 venue 秒级跳动）；**部署机实测两 venue 双流**（binance BTC 62956.9/ETH 1879.79 + okx BTC 62965 秒级推送）+ **Part A live 核账**（seed 100000/100000 + 重启幂等 1 条 capital_in + GetSimAccount RPC 返回双恒等式对平 + 历史对冲单 pnl 净 0 无漂移）；**binance 静默丢帧根因** = Go json 大小写不敏感匹配致帧内 `"e"`（事件类型字符串）撞上 `EventT`（int64），整帧 Unmarshal 失败全丢——修复补显式 EventType 字段 + 测试用真实帧形（envelope + e/E 并存）锚定；**InitSimAccount 原实现非事务（半账）已修为单事务**；**遗留事实**：binance REST ticker 采集在 throwaway 未写入（okx 正常），与 WS 无关待 deploy 观察。**M1/M2-a 追溯深审 + M3-a 施工复审全部闭环已部署**（D-035）；**M3 文档审计完成**（D-036：收敛口径修正 + G1–G5 全落 spec）；**M3-b 细化设计定稿 + 施工交付 + 复审修复 + S4 数据源修正部署验证**（D-037 + D-031 实证修订 + 本 commit：spec §9 S1–S5 全落地并已部署实测）。S1 规则→Signal 驱动（rule.OnActive 携带命中实体 + sim.Driver §3.1.1 映射表，删映射必红）；S2 8h 结算（(symbol,venue) 分组取真实市场 funding，跨 venue 污染必红）；S3 testnet 只读探针 + key 隔离（新包 internal/simtestnet，SIMULATED 缺标记拒绝加载，sim 包零网络由 domains_test 把关）；S4 历史收敛（exchange 历史 collector + 幂等回填 + sim_report 周频统计）——**数据源已修正并部署验证**（fapi.binance.com 满 365d + OKX funding-rate-history ~90d，见 D-031 修订；DB 实测 binance min_ts=2025-08-15）；S5 carry 白名单默认空（宁缺毋滥）。main.go 已接线：boot 历史回填（一次性幂等）+ simDriver（OnActive compose）+ 8h 结算循环 + testnet 探针随 settle tick；H1 洪水去重（8h 桶折叠 + Limit 2M）已含。**M3-c 细化设计定稿 + 施工交付 + 决策层复审修复**（D-038 + spec §10 C1–C5 全落地 5fd8f63；决策层复审发现并修复 repo/carry 二次门禁**恒拒**设计缺口——D-039 kind 分派数据面 + 7 对抗测试，spec §10.3/§10.4 同步）。**D-040 测试网账户区已闭环部署**（业主提问触发，对话 #49）：探针 Run 返回余额快照 → sim_testnet_accounts（migration 0006）→ GetTestnetAccounts RPC → SimExec 账户区；启动即探针一次（不等 8h tick）+ 8h tick 刷新；equity_usd 诚实口径（okx totalEq 精确 / binance 稳定币合计近似）；部署实测两路真实虚拟资金 + **sim_testnet heartbeat 首次登记已随启动探针闭环**（此前的观察项已消除）。**加密交易所出入金通道业主实测通过**（对话 #50）；TRX funding 跨所差异已核实为真实市场分歧非 bug（binance +2.3%~+2.7% 年化 vs okx −3.5%~−3.8%，实测量价一致，负 funding 不触发建单，宁缺毋滥）。**机会盘点（对话 #52）**：面板无机会 = 正确（BTC/ETH funding 4–7% 年化远低 15/20% 门槛，funding 窗口档当前不存在）；真实可交易对冲 = BTC cash-and-carry ~7%（现货+永续空，delta 中性）+ sUSDe 4.44% + 稳定币 3.3–4.2%；全市场扫描实证极端 funding 全在被砸微盘币（ACE −1495% 等，陷阱非机会，验证宁缺毋滥）；**架构确认维持轮询**（对话 #51，8h 结算尺度下事件驱动收益≈0）。**出入金通道全部闭环**：加密交易所（#50）+ **OTC 法币通道 1 万小额业主实测通过**（对话 #53）。**模拟执行演练档已部署**（D-041：funding_drill band [5%,15%)，okx BTC avg_30d=6.66% 重启即触发演练单，供确认→成交→8h 结算端到端冒烟，对话 #55）。**演练单拒单根因已修复**（D-042，对话 #56）：D-041 演练单连续拒单（UNHEDGED，ref 负值）→ 根因 = `pgstore.LatestFacts` WHERE 子句缺括号的 SQL 运算符优先级 bug（多参数查询退化为"只生效符号条件"，fundingHedgeSignal 取到 funding 负值当 ticker 价）；修复每子句加括号 + 对抗测试 TestLatestFactsFilters；另加固引擎 boot 竞态（rule.Config.BootDelay=15s + TestRunBootDelay）+ 修正 migrate 陈旧断言（want 5→6）。部署实测：**sim_orders id=3 = suggested（okx BTC ref 63063.30 spread 6.64% risk_flags={}）**——演练单可确认→成交→8h 结算全链路闭环；拒单 id=1/2 保留为负样本。**SimExec 三处体验修复已部署实测**（对话 #57 后续，业主反馈）：① 拒单提示条加手动 × 关闭入口；② 风险标记 7 个 Risk* 全量补中文徽标（UNHEDGED→未对冲 / SPREAD_LOW→价差过低 / SIZE_OVER→单笔超限 / DAILY_OVER→日额超限 / INVALID_INPUT→输入无效，SPREAD_DRIFT→漂移 / WHITELIST→未白名单保留）；③ 持仓补实时数值（proto +14 cur_price / +15 expected_ann / +16 unrealized_pnl + ListSimPositions 查 ticker/funding 计算 + PositionZone 加 4 列开仓价/当前价/预期年化/实时收益=已结算+未实现）。实测：永续空腿 expected_ann=5.53%（当前 okx funding 年化）、unrealized=+286k，现货多腿 unrealized=−286k，**两腿对冲相消=0（delta 中性 ✓，D-019 不赌原则）**；对抗测试 TestListSimPositionsRealtime 删浮动计算必红（已实测）。sim_orders id=4 拒单 SPREAD_DRIFT 20.08% 为设计内 fail-closed（生成 avg_30d 6.63% vs 确认时刻单点 funding 回落）。**确认下单面板移入监控总览页**（对话 #59，业主反馈）：新组件 ConfirmPanel（告警流卡片下方、触发器上方整宽卡片）只显示「待确认」订单 + 确认按钮（业主决策"简化一些"）；模拟执行 tab 瘦身为持仓/账户/报告（OrderZone 移除）；useSim 提升 App 层共享（ConfirmPanel + SimExec 同源，确认后两处同刷新）；共享展示辅助抽 sim.tsx（SIMULATED 徽标 + kind/side 中文映射，锚点 TestSimExecBadgeRenderable 扩展检查 sim.tsx 定义 + 两面板引用）。已部署实测（新 hash index-DxLmi9bv.js，ConfirmPanel 内容入 bundle）；**端到端验证通过**：re-arm funding_drill（rule_id=223）→ 触发 sim_orders id=5 suggested（ref 63031.8 spread 6.606%）→ ListSimOrders 正常返回（ConfirmPanel 数据源）→ ConfirmSimOrder accepted=true（二次门禁通过，年化变化 16.3%<20%）→ 持仓新增两腿（short expectedAnn=5.98%/long 0）+ 旧腿实时值正确，对冲相消=0。**确认下单卡片布局再调整已部署应用**（对话 #60，业主反馈）：ConfirmPanel 从告警流下方整宽卡改为**与机会面板同行**（`.row-col` 右列 = 告警流上 + 确认下单下，机会面板 `grid-row: span 2` 左列跨两行）；部署应用（新 hash index-DdoEXWry.js served 匹配磁盘 dist，row-col/确认下单 入 bundle）+ 全量 commit 推送 GitHub。**交付闭环教训入 practices #19**（§7.3 自审 ≠ 交付终点：运行态改动 check-out 必须「构建 → 部署应用 → 推送」三步闭环，本地 commit 不代表上线）。**机会面板 defi 矩阵横向滑块已修**（对话 #61，业主提问触发）：根因 = 5 个 DeFi 协议名当矩阵列头（14~16 字符 nowrap）撑宽；修法 MatrixTable 加 colLabel + Opportunity venueLabel 映射（Aave/BlackRock/Ethena/Morpho/Ondo，practices #18 全集对照）；部署应用（index-CJDJqr2V.js served 匹配）。**进化建议引擎 L0 已部署应用**（D-044，对话 #62）：总览页触发器下方新增「进化建议」卡（reject_dist 拒单分布 / defi_anomaly 利率尖峰 / no_order 连续无单 / source_down·stale 停更四信号，中位数×2.0 因子检测，只读证据表面永不自动改规则，action 一律走 D# 人工决策）；部署实测 reject_dist（UNHEDGED ×2/SPREAD_DRIFT ×1）+ source_stale ×3 正确出现，no_order 被近 7 天 filled 单正确抑制，defi_anomaly 因 Aave 12.57% 尖峰已回落至 3.29%（最新截面平稳）正确不报。**carry + repo 完整接入模拟盘已部署应用**（D-045，对话 #63）：结算数据面按腿 kind 分派（settleFactKind：funding_hedge→funding / carry_asset→defi_rate / repo→reverse_repo，settleOnce 按 (kind,symbol,venue) 分组 + SettleFunding 带 kind 维）——此前 settleOnce 只查 funding 事实，carry/repo 腿"建了仓也永不生息"；repoSignal 落单 venue/symbol 取事实真实值（sina/GC001，不再硬编码 domestic/GC001）；carry 独立低门槛 CarryMinSpread（默认 1%，纠正 funding 摩擦假设误用于持有生息的口径错配，非放宽门禁造数据——repo 5% 时点意图/funding 15% 不变）；生产 env 新建 /etc/arbcn/arbcn-monitor.env（此前 systemd `EnvironmentFile=-` 指向但文件缺失 = 全默认运行）注入 `ARBCN_SIM_CARRY_WHITELIST=SUSDE,USDE,BUIDL,STEAKUSDC,USDY`。对抗测试 3 个新锚点（删分派/改回硬编码/删分档必红已实证）+ config_test 扩展；全量测试/vet/-race 绿；部署重启实测 active + 新二进制 inode 匹配 + served bundle 一致。**诚实标注**：carry 单是否真触发取决于 defi 池出现 ≥0.5%/h 变动（不造数据）；repo 当前 0.865% < 5% 会被 SPREAD_LOW 拒（负样本，符合时点逆回购意图）。**D-046 机会实算卡 + 市场结构经验库已部署应用**（对话 #64，业主核心追问「投运后系统如何自己算账」触发）：实算卡 = 确定性公式（瞬时/30日均值/保本天数 `f×365/Inst`/扣摩擦净年化 `Avg30−f×365/30`/三档判定 vs 稳定币基档 4.5% D-021/中文模板叙述 D-043），纯函数 oppcalc.go + `ListOppCards` RPC（dashboard 域，快照第 8 路），**摩擦 0.3% 业主已核实普通主户**（facts.md 落档，env `ARBCN_OPP_FRICTION_FUNDING` 可配）；经验库 = migration 0007 `knowledge_entries` + `internal/knowledge` 包（签名探测器纯函数 + 3 条 seed）+ `knowledge_match` 只读 insight（信号 5）+ `ListKnowledgeEntries` RPC + 前端「机会实算卡」区块（评级徽标 + 数值瓦片 + 摩擦明示）+「市场结构经验库」卡（KnowledgeBoard）+ Insights「knowledge→经验」；**只读证据不碰任何执行门禁**（D-016/MinSpread/CarryMinSpread/白名单不动），**吸收=人工 D# 落盘永不自动**（practices #20/#23）。对抗测试 3 组锚点（删公式/删匹配/删中位数因子必红）+ 4 处 test fake 补 Store 方法；全量测试/vet/npm build 绿；部署重启 + migration 0007 自动应用；实测预期：ETH@okx 瞬时 8.82% vs 均值 ~4% 判尖峰陷阱卡、经验库 3 条 seed、knowledge_match 命中。**前端第一性原则审计修复已部署应用**（D-047，对话 #65，业主发起审计 + 拍板 P0+P1+P2 全修）：P0 数据 hook 随视图生命周期——新增 OverviewPage/FactsPage/SimPage 三页面组件承载各自数据 hook（hook 生命周期 = 视图生命周期，切 tab 即卸载停轮询，消跨 tab 空转），App 根仅留 useSnapshot（header 全局 chrome）+ `refreshKey` 信号（顶部刷新 = reload + 递增，联动总览页 sim/knowledge 重载，修「刷新只刷一半」）；useSim 加 60s 轮询（8h 结算新单进确认面板，修新鲜度脱节）。P1 双源映射收敛——kindText 单源（Opportunity 删本地 kindLabel）+ grep 锚点测试 TestSimKindLabelCoverage（红→绿已实证）；covered 单源在后端（proto FactRmb.covered + rmb.Converted.Covered），前端删 COVERED_KINDS。P2——F4 ListSimPositionsResponse.fx_available presence flag（前端删 pnl_rmb=0 启发式，真零 PnL 不再误标「USD 原值」）；F3 删 freshness 死分支；F5 ledgerDate 移 format.ts；F6 抽 FreshDot 共享组件；F7 Alerts/Bell 行保持（D# 记 rationale）。**设计回归留痕**：对话 #59 曾提升 useSim 到 App 层共享（「确认后两处同刷新」），本次下沉后 ConfirmPanel 确认 → 切 sim tab → SimPage 挂载即重拉最新，天然覆盖，回归「数据随视图」。对抗测试 + 全量测试（含真库 DSN）/vet/npm build 绿；部署重启（SIGKILL 既有模式，sudo 交互受阻）+ 实测 healthz ok + 新二进制 inode 匹配（stat -L）+ served bundle == 新 dist（index-c9bH9JKr.js）+ ListFacts covered 18/32 + ListSimPositions fx_available=true + 4 持仓。**总览页布局按「第一眼原则」重构已部署应用**（D-048，对话 #66，业主指示「UI 第一眼最需要看到什么决定布局」）：U1 确认下单置顶整宽（该我行动永远第一眼可见，空态保底不消失——保住错误横幅/刷新出口）；U2 机会实算卡移面板顶部（裁决先于数据），funding/defi/IV/逆回购四数据块包 Collapse（funding 默认展开、低频面折叠下钻）；U3 经验库折叠（knowledge_match 命中才默认展开，判定记录供裁决对照）；U4 触发器仅 active 显示 + 全量规则表折叠；新组件 Collapse（原生 `<details>` 受控 open，三处复用零 JS 依赖）+ 删 `.row-col`/`span 2` 死样式 + Insights 提序；**纯前端布局，零执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**；grep 锚点恒绿（Opportunity 仍 import kindText、ConfirmPanel 仍引 SimulatedBadge）；部署实测 healthz ok + 新二进制 inode 匹配（stat -L，21244274→21244337）+ served bundle == 新 dist（index-DTovgGTO.js）。**总览页布局第 2 版（3×2 网格）已部署应用**（D-050，对话 #68，业主不满意 D-048 指定精确网格 + 「打开首页第 1 版就能完成基本的信息和操作」）：左上1 市场数据矩阵（funding/defi/IV/逆回购**全展开**，眼睛扫描起点）/ 右上1 经验库（defaultOpen）/ 左2 机会面板（实算卡，矩阵已拆走 = 缩短）/ **右2 确认下单（鼠标顺手高度操作热区）** / 左3 进化建议 / 右3 触发器；机会面板拆分（新 MarketMatrix.tsx + Opportunity 实算卡瘦身）；**告警流移入铃铛**（第1性原则：第一眼要「状态」不要「内容」——未读数徽标 header / 时间线+ack 拉取式，删 Alerts.tsx + `.timeline` 死 CSS + OverviewPage 删 ackBusy/ack props）；D-048 U2「裁决先于数据/确认置顶」**反转留痕**；纯前端零门禁改动；锚点恒绿 + 全量测试/vet/npm build 绿；部署实测 healthz ok + 新 inode（21244337→21244374）+ served bundle == 新 dist（index-Bkl8ybxZ.js）。**移动/小屏自适应已部署应用**（D-051，对话 #69，业主指令「有做移动小屏自适应吗？这个需要做」）：已有桌面优先兜底（viewport meta + .grid 860px 折叠 + 铃铛 600px 全屏 + table-scroll + 瓦片 auto-fill + flex-wrap），本次补真移动适配——600px 断点（与铃铛一致）触摸目标 ≥44px / body 15px / 输入 16px iOS 防缩放 / 表格惯性滚动 / 卡片+main 留白收紧 / safe-area；480px 台账表单单栏；touch-action:manipulation 消双击缩放 + hover:none 按压反馈；**375px 无头 chromium CDP 实测抓到并修复两个真溢出 bug**：① .grid 860px 折叠 `1fr` 隐式 auto 最小被宽矩阵撑破视口（docScrollWidth 594）→ 改 minmax(0,1fr)；② 480px `.ledger-note{1/-1}` 特异性 (0,1,0) 不敌基础 `.ledger-form .ledger-note{span 2}` (0,2,0) 撑出隐式第二列（docScrollWidth 511）→ 媒体块改同特异性后代选择器；修复后三档视口全过（375px 单栏无溢出+44px 目标 / 768px 单栏无溢出+按钮不被污染 / 1280px 双栏 3fr/2fr 无溢出+14px 字号）；纯前端 CSS 零门禁改动；锚点恒绿 + 全量测试/vet/npm build/tsc 绿；部署实测 inode 21244397 + served bundle == 新 dist（index-CRNqUktn.js / index-Cjd-E_-V.css）。**D-052 长卡片全局高度协调 + 确认下单提上去已部署应用**（对话 #70，业主五连问触发）：机会面板/经验库/进化建议三处列表接入新 `.scroll-cap`（max-height:min(68vh,520px)+overflow-y:auto，600px 内 min(52vh,360px)）= **卡内滚动代替分页**，机会面板 3092px→618、整页 4781→2305；矩阵表加 440px 封顶护栏（当前各表 <440 不生效，未来加币种防延伸）；**确认下单提上去**（DOM 重排第 4→第 2，grid auto-flow 视觉序 = DOM 序）——桌面 = 右上1 首行（鼠标顺手度优于原右2）、移动单列 = 第 2 位（不再被机会面板顶到底）；「待复核永不自动复核」已核实（pgstore UpsertKnowledgeEntry 不写 validated_at + ON CONFLICT DO NOTHING，人工 D# 复核）；实测 375 移动 docSW==innerW 无溢出 + 桌面 6 卡各排对齐；锚点恒绿 + 全量测试/vet/npm build/tsc 绿；部署实测 inode 21244415→21244416 + served bundle == 新 dist（index-DQsLtvVN.js）。**D-053 布局第 3 版 + D-054 经验库人工复核已部署应用**（对话 #71，业主指定「右1 双卡堆叠等高矩阵」+「进化建议右2 等高机会面板」+ 问「如何复核」）：`.right-stack` flex column 单网格 cell 包经验库（上）+ 确认下单（下）+ 删 `.grid` 的 `align-items:start`（恢复默认 stretch）→ **right-stack 自动 = 矩阵高 875px、进化建议自动 = 机会面板高 618px，零逐卡高度规则**；DOM 序 = 矩阵 → right-stack(经验库+确认下单) → 机会 → 进化建议 → 触发器；**复核 = 人工在环闭环**（D-054：新 RPC ReviewKnowledgeEntry + Store 方法——signature 必填 + status 三态白名单 + verdict 自由文本留空=COALESCE 保留原判定 + validation_note 必填留痕；**verdict/status 绝不混写**，初稿自审纠正；只改判定记录不触门禁）+ 前端 KnowledgeBoard 每行「复核/再次复核」按钮 → 内联表单（状态 select + 判定文本留空=保留 + 必填结论 + 确认/取消）；实测 1280 matrix h875==right-stack h875（经验库 603+确认下单 119）、opp h618==insights h618、375 无溢出、RPC 三条拒绝路径 live 命中（空 note/非法 status 400 + 未知签名 503）+ CDP 复核表单渲染（select 三态/verdict 预填「坑·核实」/note 必填）；锚点恒绿 + 全量测试/vet/npm build/tsc 绿；部署实测 inode 21244442 + served bundle == 新 dist（index-BRFIYzsI.js / index-_h0_Ebl1.css）。**D-055 模拟盘平仓补齐 + 浮动收益列已部署应用**（对话 #72，业主「只做了开仓，平仓没做就做了 1/2 + 持仓要显示浮动收益」）：**平仓 = 订单级整单平**（D-019 绝不单腿）——migration 0008（sim_orders.status CHECK 加 closed，部署自动应用）+ store.CloseSimOrder（单事务 filled 守卫 → 逐腿 pnl+=当前价浮动 + settled（腿须属本单且 open，任一 miss 回滚）→ 订单 closed + note）+ CloseSimOrder RPC（前置 status != filled → FailedPrecondition 双层防重复平；AddPnl = (cur−ref)×qty×dir 复用 ListSimPositions 浮动口径，ticker 缺失 add=0 不编造；realized_pnl = 各腿 (pnl+add) 合计 / realized_rmb 即期折算缺失=0）+ 前端持仓表独立「浮动收益」列（当前价缺失标 —）+ 每订单首腿「平仓」按钮（二次点击确认 armed 警示高亮 rowSpan 整单表达 + 成功横幅显示实现净 PnL）+ settled 腿移出持仓表（持仓 = 当前持有）；**预期收益答疑**：expected_ann 是实时投影（当前 okx BTC funding 年化 4.899%）非开仓锁定值，funding 从 ~8% 回落到 ~4.9% = 市场条件变化非 bug（facts.md 证据 8/15 20:42→8/16 现时）；实测部署：healthz pending_migrations→ok（0008 应用）、inode 21244442→21244499、served bundle == 新 dist（index-EdR3ou5I.js 含「确认平仓/浮动收益/closeSimOrder」）；零执行门禁改动。**D-056 收尾 + D-057 测试事故修复已闭环**（对话 #73→#76）：**事故**——上会话 `go test` 的 `ARBCN_TEST_PG_DSN` 误指向**真实库 arbcn**（应专用测试库 arbcn_test），pgstore 测试 `resetTables` 对 sim 四表 TRUNCATE → 清掉真实 drill 单 1-5（不可恢复）+ 遗留测试行：订单 16/17（src_rule 空、sim_local）、流水 7-13（open ±6e8 = 10000×60000，TestCloseSimOrder 数据）、sim_account `capital=0 / cash=123.45` 卡死 0；**根因双修**——① `testPool` 安全闸（pgstore_test.go：ARBCN_TEST_PG_DSN 的 dbname 不含 "test" → t.Fatalf 拒绝执行，migrate_test 的 DROP 同受保护；负测验证：DSN 指向 arbcn 立即拒绝且零触碰）+ ② `InitSimAccount` 补正（sim_cash.go：applyCashFlow 兜底 upsert 先以 capital=0 建行、ON CONFLICT DO NOTHING 永不补本金 → 改 ON CONFLICT DO UPDATE 补正 capital=0 + 仅当无 capital_in 流水才入金幂等，对抗测试 TestInitSimAccountRepairsUnfunded 删补正/删入金必红）；**清理 + 重启复位**：业主确认后删除测试残留（保留真实订单 18 funding_drill @ binance 拒单），重启 seed 100000/100000 + 1 条 capital_in，Fix-2 二进制（inode 21244553）重启幂等（capital_in 仍 1 条）+ GetSimAccount RPC 双恒等式对平 + served bundle == dist 不变 + healthz ok。**D-058 实盘推进路线定稿（盈利验证优先，对话 #77–#79 业主两次纠偏）**：业主最终目标 = 实盘真实资金交易；此前三阶段含「阶段 A-0 testnet 执行层验证」——业主纠偏「第 1 原则是盈利，策略不盈利执行机制无用；本地模拟盘能校验盈利能力就不必浪费时间到 API 接入；确认能盈利再接入 testnet 不晚」+「模拟都亏损说明路不通」→ **路线重排：阶段 0（现在，无 API）= 本地模拟盘盈利性验证**（真实候选 BTC cash-and-carry ~7% 等经 D-041 演练档进模拟，D-056 账本 + D-055 平仓 + D-037 真实费率结算，积累 paper PnL）→ **判定门 ①**（跨 30 天窗口 paper 净年化 ≥ D-026 诚实基线 3.2–3.7% + 摩擦余量才进阶段 A，为负/不足即止投）→ **阶段 A 真金冒烟 ~1 万 RMB 级**（摩擦后真实盈利唯一裁决）→ **阶段 B/C 按证据放大**；**testnet 从必经降为可选预演**（冒烟前想假钱演练「下单→回读→记账→对账」全流程才用，非必经随时可补）；**D-010/D-034 维持原状**（testnet key 只读探针用途不扩下单，真金零密钥）。
- 方向校验: 与 AGENTS.md §1 一致 —— 不赌原则（D-019）+ 收益最大×路径最短（D-020）+ 加密三档（D-021）+ 敞口知情（D-023）+ 无密钥铁律（D-010/§13，M3-a 复审复核：sim 包零网络零密钥）。**进化建议引擎 L0（D-044）守住边界**：只读证据表面，永不自动改规则（决策在环，D-019/D-043 同源）。**D-059 复核自动证据守住边界**：系统只呈现「当前数据证据」、永不自动改判定/吸收——「自动验证」= 算证据（复用 D-046 已核实探测器），裁决 = 人工在环点击写 validated_at（D-054/practices #20 同源强化而非违背）；证据缺失给「无法自动核验」不编造、数据面故障降级不阻断浏览（浏览是主、证据是辅）；ClosePanel 整单平强化而非违背 D-019（对冲结构订单全部腿一起退，绝不单腿裸敞口），确认/平仓仍 SIMULATED 人工触发；订单历史只读呈现拒单负样本（门禁 fail-closed 是设计内，记录供复盘、永不自动重试）；纯前端 + RPC 响应字段，**零执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**，方向无漂移。**D-045 carry+repo 接入守住口径**：carry/repo 均 D-021 已定义档位（非新方向）；结算仍用真实市场公开事实（非 testnet，D-037 同口径）；CarryMinSpread=1% 是纠正 funding 摩擦假设误用于持有生息的口径错配，非为数据放宽门禁。**D-046 机会实算卡 + 经验库守住边界**：实算卡 = 确定性公式写死（「算账不是聪明是公式」，D-043 不接 LLM），只读证据不碰任何执行门禁（D-016 15%/20%、MinSpread/CarryMinSpread、carry 白名单不动）；经验库吸收 = 人工 D# 落盘（git 跟踪）、匹配 = 签名纯函数、呈现 = 只读，系统永不自动吸收/自动改判（practices #20 同源）——「自我进化」= 越用越多（新情况逐步 D# 吸收），非自动改规则。**D-047 前端审计修复守住边界**：纯前端重构 + RPC 响应字段（FactRmb.covered / ListSimPositionsResponse.fx_available），**零执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**；数据获取层随视图收缩 = 纯优化（省空转）不改变任何决策语义；设计回归（对话 #59 useSim 提升 → 本次下沉）走 D# 留痕（§1 首行锚：方向改动必须 decisions.md 留痕）。**D-048 总览页布局重构守住边界**：纯前端布局 + 折叠交互（Collapse），**零执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**（U 项全程不碰）；不接 LLM（D-043）、不赌（D-019）——确认下单仍 SIMULATED + 人工确认；布局第一眼层级（行动→裁决→信号→数据）直映业主 UI 第一性原则，方向无漂移。**D-050 布局第 2 版守住边界**：纯前端布局 + 组件拆分（MarketMatrix/Opportunity 瘦身）+ 告警流移铃铛，**零执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**；第一性 = 信息架构（眼睛扫描起点=左上，数据矩阵置顶）+ 操作热区（鼠标顺手高度=中右，确认下单）+ 告警流状态优先（徽标→内容拉取式），与 §1「决策监控 + 记账」定位一致；D-048 U2 反转走 D# 留痕，方向无漂移。**D-051 移动/小屏自适应守住边界**：纯前端 CSS 媒体查询增量（style.css），**零 JSX/后端/RPC/执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**；移动适配 = 可用性工程（触摸目标/字号/iOS 缩放/惯性滚动），不改变任何决策语义；实测抓的两个 bug（grid 1fr auto-min 撑破视口 / ledger-note 特异性）是纯渲染层缺陷，与业务逻辑无涉，方向无漂移。**D-052 高度协调 + 提上去守住边界**：纯前端（style.css 封顶 + OverviewPage DOM 重排），**零执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**；封顶/提权 = 阅读可用性工程（消除无限延伸 + 行动块上移），不改变任何决策语义；「待复核」核实结论（人工复核永不自动）与 practices #20 同源，系统仍未自动改判，方向无漂移。**D-053 布局第 3 版守住边界**：纯前端（.right-stack 单 cell 包装 + grid 默认 stretch 等高），**零执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**；等高 = 网格行高自然推论（非硬编码），不改变任何决策语义；确认下单仍在首屏（右1 堆叠下半），「该我行动第一眼可见」延续，方向无漂移。**D-054 人工复核守住边界**：复核 = **决策层人工在环**（owner 行为），系统永不自动复核（practices #20 同源）；RPC 只写判定记录（validated_at/status/verdict/note），**零执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**；不接 LLM（D-043）、不赌（D-019）；「决策资产由决策层更新」= 强化而非违背 §1（决策监控 + 记账、经验吸收=人工 D# 落盘），方向无漂移。**D-055 平仓守住边界**：平仓 = 模拟盘对称补全（开仓/平仓双写路径齐，非新方向），**订单级整单平强化而非违背不赌原则**（D-019：对冲结构单腿平 = 裸敞口，全部腿一起退才 delta 中性延续）；AddPnl/realized 口径复用既有 ListSimPositions 浮动与即期折算口径（P3 单一真相源，无新口径）；ticker 缺失 add=0 宁缺毋滥不编造；迁移只扩 CHECK 枚举（closed）不改任何执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单；平仓仍人工触发、模拟盘无真实资金路径（§6/§8），方向无漂移。**D-056 现金账本 + 实时报价守住边界**：现金账本 = 模拟盘记账补全（业主「按真实账户对待」），现金流 open/funding/close/capital_in 全在既有事务内原子入账，**记账不产生任何执行/门禁/规则/阈值改动**（D-019 不赌——对冲单两腿现金流相消净 0，等价 class 不变量 cash+Σopen(dir×qty×cur)=capital+realized+unrealized 代数验证 + 历史对冲单 pnl 净 0 实测无漂移）；equity 双恒等式 = 记账口径（一套账两种展开），非新收益模型；资金费仍走真实市场公开 funding（D-037 同口径），不因账本接 real 测试金。报价 = **只展示不喂策略不落库**（诚实标注，facts ticker 1min 仍 equity/positions 真相源），公共无密钥端点（D-010），上游 WS/下游 SSE 均无自动执行（D-013/D-016 门禁不动），不赌（D-019）无敞口；gorilla/websocket 唯一新依赖 = WS 客户端无标准库替代（B 实现）；「秒级」是看盘体验面，不改变任何决策语义，方向无漂移。**D-057 事故修复守住边界**：testPool 安全闸 + InitSimAccount 补正 = 测试基建安全与账本健壮性修复，**零执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**（D-019 不赌不动）；账本复位 = 删除测试污染行恢复干净 seed，记账口径原样（equity 双恒等式不变）；测试仍只指向专用测试库，不产生任何新执行/决策路径，方向无漂移。**D-058 盈利验证优先守住边界**：第 1 原则是盈利（D-019 不赌同源——不赚不做的套利才是不赌；策略不盈利执行机制无用）；本地模拟盘用真实行情 + 真实费率验证策略层理论盈利（D-037 结算口径，非 testnet 假费率）是第一道闸且不需要 API；**testnet 从必经降为可选预演**（执行机制 bug 学费用小资金冒烟一次付清，有界、delta 中性、风险封顶冒烟资金内）；**D-010/D-034 零变更**——testnet key 只读探针用途不扩下单，真金执行维持零密钥；「模拟亏损 = 路径不通止投」强化 D-019 不赌与宁缺毋滥，方向无漂移。**对话 #82 画布加宽守住边界**：纯前端 CSS（main max-width + grid fr 比例 + review-actions grid-column），**零 JSX/后端/RPC/执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**；画布宽度/列比例/复核表单落位均属可用性呈现层（D-050/D-052/D-053 布局演进延续），不改变任何决策语义，方向无漂移。**D-060 复核方向快照守住边界**：系统**永不裁决、只比较**——方向快照在复核时刻由人工在环写入（validated_at 仍只由 ReviewKnowledgeEntry 人工调用写，practices #20 同源），之后每次对当前数据重算方向、与快照布尔比较：翻转 = 「建议复核」警示、一致 = 「仍适用」，改判仍人工 D# 复核提交；「待决策层清单」= 翻转条目的聚合呈现（D-052 同源第一眼），非自动处理；不做 B(LLM) 理由已落 D-060（判断力错配——布尔比较无需智能、成本复杂度、责任不可归属，D-043 缺口维持开放）；数据面不可判定时保留旧快照不覆盖历史（诚实：无基线 = 「已复核」不虚称「仍适用」）；**零执行门禁/规则/阈值/D-016/MinSpread/CarryMinSpread/白名单改动**，不赌（D-019）不接 LLM（D-043），方向无漂移。
- 施工表:
  | 子任务 | 状态 | 锚点 |
  |--------|------|------|
  | 脚手架 + 协议 + 门禁 | ✅ | 5bea136 / D-001~D-005 |
  | charter 正式化 ~ 不赌原则 | ✅ | D-008~D-019 |
  | 优先级反转：监控先行 | ✅ | D-020 |
  | 加密三档结构 | ✅ | D-021 |
  | 国内 vs 加密对比 + 敞口知情 | ✅ | D-023 |
  | 方案再审计：动态路由 + 阶梯三档 + 出入金设计 | ✅ | D-024 |
  | 保本凸性档（CPPI 式） | ✅ | D-025 |
  | 档位定稿（保本凸性 50% + 稳定币 50%） | ✅ | D-026 |
  | 第 3 轮审计：期权动态预算 + 自托管腿 | ✅ | D-027 |
  | 市场事实知识库 facts.md + 数据闭环 | ✅ | D-028 |
  | 监控系统架构设计 | ✅ | D-029（02-monitor-architecture.md） |
  | Go 裁决 + 客户端形态 + 三问确认 | ✅ | D-030 |
  | **M1-a 脚手架 → 施工 agent #1** | ✅ | e999f8e |
  | **M1-b Fact 模型+存储层 → 施工 agent #2** | ✅ | e04aa4b |
  | **M1-c Exchange collector → 施工 agent #3** | ✅ | c1accb2 |
  | **M1-d DeFiRates/Domestic/Calendar/IV → 施工 agent #4** | ✅ | c8b912d |
  | **M1-e 规则引擎+对抗测试 → 施工 agent #5** | ✅ | 505d2af |
  | **M1-f Alerter+元监控 → 施工 agent #6** | ✅ | 88dbf9e |
  | **M1-g Web 仪表盘最小集 → 施工 agent #7** | ✅ | bb8c399 |
  | **M1-h 全链路联调+A-F 自审 → 施工 agent #8** | ✅ | 6cefbea |
  | **M1 验收（决策层）** | ✅ | dialogue #29 |
  | **M1-i SMTP 降级补丁 → 施工 agent #9** | ✅ | 0d740b7 |
  | 部署 systemd 常驻（mluser 运行） | ✅ | 2026-08-15 实测 active |
  | 加密交易所出入金通道验证 | ✅ | 业主实测通过（2026-08-16，dialogue #50） |
  | 出入金通道验证（1 万小额 OTC 法币通道） | ✅ | 业主实测通过（2026-08-16，dialogue #53） |
  | **M2-a 后端：3 RPC（ListUnacked/AckAll/ListSourceHealth）+ 调度去重 → 施工 agent #1** | ✅ | e26eea9 |
  | **M2-a 前端：铃铛通知中心 + freshness 徽标 → 施工 agent #2** | ✅ | 2afac33 |
  | **i18n：页面硬编码英文 → 中文（前后端一致 ruleLabel/消息模板）** | ✅ | 34ae607 |
  | **M2-b：RMB 折算 + facts.md 自动导出 + 台账 → 施工 agent** | ✅ | 复审修 F1/F2 |
  | **M1/M2-a 追溯深审（R1-R6 六路 review + 修复 14 项）** | ✅ | D-035 |
  | **M3-a：订单生成器 + 本地模拟盘回填** | ✅ | D-034/D-035 |
  | **M3-a 复审修复：H1 结算 100× / M1 成交原子 / M3 NaN 门禁 / L1-L3** | ✅ | D-035 |
  | **M3-b S1：规则→Signal 驱动接线（rule.OnActive 携带命中实体 + sim.Driver 映射表）** | ✅ | sim/driver.go + driver_test.go（删映射必红） |
  | **M3-b S2：8h 结算调度（(symbol,venue) 分组取真实 funding）** | ✅ | sim/driver.go settleLoop + TestSettleByVenue |
  | **M3-b S3：testnet 只读探针 + key 隔离（新包 simtestnet，SIMULATED 门控）** | ✅ | internal/simtestnet/ + domains_test |
  | **M3-b S4：历史收敛（exchange 历史 collector + 幂等回填 + sim_report 周频）** | ✅ | collect/exchange/history.go + sim/report.go（部署验证：binance 满 365d / OKX ~90d，D-031 修订） |
  | **M3-b S5：carry 白名单 + 降级（默认空宁缺毋滥）** | ✅ | sim/config.go CarryWhitelist |
  | **M3-c 细化设计定稿** | ✅ | D-038 + spec §10（C1–C5，含 proto 全文/RPC/门禁/锚点） |
  | **M3-c C1：SimService proto + 生成物** | ✅ | proto/arbcn/sim/v1/sim.proto + buf.gen.sim.yaml（独立域；dashboardv1 生成物 byte-identical 未动） |
  | **M3-c C2：SPREAD_DRIFT 二次门禁** | ✅ | sim/order.go RiskSpreadDrift + sim/confirm.go ConfirmDriftCheck（G5 口径 + fail-closed） |
  | **M3-c C3：确认成交流（ConfirmSimOrder 唯一写路径）** | ✅ | simapi/service.go + pgstore Accept/RejectSimOrder（suggested 守卫原子） |
  | **M3-c C4：模拟执行 UI tab** | ✅ | App.tsx 第 4 tab + SimExec.tsx（SIMULATED 徽标 + 即期 RMB） |
  | **M3-c C5：可检查性 + main.go 接线 + 验收** | ✅ | domains_test simapi grep 断言 + mux 接线 + go vet/test -race |
  | **M3-c 复审修复：repo/carry 二次门禁恒拒（D-039 kind 分派）** | ✅ | D-039 + spec §10.3/§10.4 + simapi/confirmDrift + 7 对抗测试（删分派必红） |
  | **M3-c 部署验收（spec §10.6）** | ✅ | 343f6a6 部署实测：重启 healthz ok + SimService 2 RPC 200 + dashboard 零回归（ListFacts 真实数据）+ 前端 index 200；确认流单测 8 场景全覆盖（真实 suggested 订单端到端冒烟依赖规则命中，库空时以单测+连通冒烟替代，诚实标注） |
  | **S3 testnet 探针启用（业主 key）** | ✅ | /etc/arbcn/arbcn-sim.env（mluser:mluser 0600，SIMULATED+全量 key）；binance/okx 连通 curl 实测 200；OKX ts ISO 格式探针 bug 修复（50102，practices #12）；首次 heartbeat 待 8h tick |
  | **D-040 测试网账户区（探针余额持久化 + RPC + UI）** | ✅ | migration 0006 sim_testnet_accounts + store 两方法 + probe Run 返回快照（binance 稳定币近似/okx totalEq）+ GetTestnetAccounts RPC + main 启动探针持久化 + SimExec 账户区（诚实口径标注）；部署实测两路真实虚拟资金 + 启动探针 heartbeat 登记（对话 #49） |
  | 告警流高度封顶修复（触发器脱节） | ✅ | web/src/style.css .timeline max-height:min(60vh,480px)+overflow-y:auto；部署实测（对话 #54） |
  | **D-041 模拟盘 funding_drill 演练档** | ✅ | defaults.go 规则（band avg_30d>5&&<15, Info）+ driver 映射 + 对抗测试 + spec §3.1.1；okx BTC avg_30d=6.66% 部署后即触发（对话 #55） |
  | **D-042 演练单拒单根因修复（LatestFacts SQL 优先级 + boot 竞态加固）** | ✅ | dashboard.go 括号修复 + TestLatestFactsFilters（删括号必红）+ Config.BootDelay=15s + TestRunBootDelay（删 sleep 必红）+ migrate 陈旧断言修正；部署实测 sim_orders id=3=suggested（对话 #56） |
  | **SimExec 三处体验修复（提示条关闭 / 风险标记中文 / 持仓实时数值）** | ✅ | SimExec.tsx riskLabel 7 全量中文 + banner-close 关闭按钮 + PositionZone 4 列；proto +14/+15/+16 + codegen + ListSimPositions 实时字段 + TestListSimPositionsRealtime（删计算必红）；部署实测对冲相消=0（对话 #57 后续） |
  | **确认下单面板移入总览页（ConfirmPanel + sim.tsx 共享 + useSim 提升）** | ✅ | ConfirmPanel.tsx（待确认订单+确认按钮，简化 6 列）+ sim.tsx（徽标/文案共享）+ App.tsx 布局（告警流下方）+ SimExec 瘦身（props）+ TestSimExecBadgeRenderable 扩展（sim.tsx 定义 + 两面板引用）；部署实测 hash 匹配 + 内容入 bundle（对话 #59） |
  | **确认下单卡片布局调整（与机会面板同行，.row-col 右列）** | ✅ | App.tsx 双栏改造（机会面板 grid-row span 2 + `.row-col` = 告警流上/确认下单下）+ style.css .row-col flex；部署应用（index-DdoEXWry.js served 匹配）+ 推送 GitHub（对话 #60） |
  | **进化建议引擎 L0（D-044：四信号只读证据表面）** | ✅ | proto Insight/ListInsights + buf generate + `internal/dashboard/insights.go`（rejectDistribution/defiAnomalies/noOrderHint/median 纯函数 + ListInsights RPC 编排，中位数×2.0 因子、NaN 守卫 practices #7）+ insights_test.go 对抗（删判定/删计数必红已实证）+ fakeStore ListSimOrders 真语义 + 前端「进化建议」卡（触发器下方整宽卡，severity 色 + 类别中文 + actions 走 D#）；部署实测（index-CtjddyB-.js served 匹配，reject_dist UNHEDGED ×2/SPREAD_DRIFT ×1 + source_stale ×3 正确，no_order 被近 7 天 filled 单抑制，defi_anomaly 因最新截面平稳正确不报） |
  | **D-045 carry + repo 完整接入模拟盘（结算分派 + 门槛分档 + venue 对齐）** | ✅ | driver.go `settleFactKind`（funding→funding/carry→defi_rate/repo→reverse_repo）+ settleOnce 按 (kind,symbol,venue) 分组分派 + backfill.go SettleFunding 带 kind + config.go CarryMinSpread（默认 1%/env/NaN 拒载）+ order.go carry 门槛分档 + repoSignal venue/symbol 取事实真实值 + 生产 env 白名单（SUSDE,USDE,BUIDL,STEAKUSDC,USDY）；对抗测试 TestSettleDispatchByKind/TestSettleRepoUsesRealVenue/TestCarryUsesCarryMinSpread（删分派/改回硬编码/删分档必红已实证）+ config_test 扩展 + TestDriverRepoBuildsOrder 事实带真实 venue；部署实测（新二进制 inode 匹配 + 启动干净 + bundle 一致，对话 #63） |
  | **D-046 F1 机会实算卡（确定性算账，投运后自算）** | ✅ | `internal/dashboard/oppcalc.go` 纯函数（OppCard/fundingCard 公式 `BreakEvenDays=f×365/Inst` `NetAnnualized=Avg30−f×365/30`/carryCard/repoCard/ratingFor 三档 vs stableBasePct 4.5%/中文模板叙述 D-043/NaN 显式守卫 practices #7）+ `oppcalc_rpc.go`（ListOppCards + oppCards 编排 3 数据面 + oppFriction env）+ proto OpportunityCard/ListOppCards + Service.OppFrictionFunding（默认 0.3，业主已核实普通主户）+ main.go env 接线；对抗测试 oppcalc_test.go（删公式/删尖峰分支/删编排必红）+ 前端「机会实算卡」区块（评级徽标 grab=绿/打平=黄/坑=红 + 瞬时/30日均值/保本天数/净年化瓦片 + 摩擦明示 + 叙述）+ hooks snapshot 第 8 路 RPC |
  | **D-046 F2 市场结构经验库（吸收=人工 D# / 匹配=纯函数 / 呈现=只读）** | ✅ | migrations 0007 `knowledge_entries`（id BIGSERIAL PK + signature UNIQUE，ON CONFLICT 镜像 rules.name）+ store.KnowledgeEntry + pgstore/knowledge.go（Upsert 镜像 UpsertRule）+ `internal/knowledge` 包（3 签名常量 + FundingSpikeTrap/DefiPoolSpikes 中位数×2/CrossVenueDivergence minSpread=4.0 纯函数 + Defaults 3 seed + 对抗测试）+ dashboard/knowledge.go（ListKnowledgeEntries RPC + knowledgeMatches 信号 5 接线 insights.go）+ proto KnowledgeEntry/ListKnowledgeEntries + 4 处 test fake 补 Store 方法；前端 KnowledgeBoard.tsx（状态徽标 + 复核标记）+ Insights catLabel「knowledge→经验」+ useKnowledge 低频加载；只读证据不碰门禁、永不自动吸收（practices #20/#23） |
  | **D-047 前端第一性原则审计修复（P0 数据随视图 + P1 映射收敛 + P2 小项）** | ✅ | P0：OverviewPage/FactsPage/SimPage 三页面组件 + hooks.ts（useSim/useKnowledge 加 refreshKey + useSim 60s 轮询 + alive 守卫 + 返回 fxAvailable）+ App.tsx 重写（仅 useSnapshot + refreshKey 信号 + 三页面分派）；P1：sim.tsx kindText 单源 + TestSimKindLabelCoverage（grep 锚点红→绿）+ proto FactRmb.covered + rmb.Converted.Covered + toFactRmb（前端 FactsSnapshot 删 COVERED_KINDS）；P2：proto ListSimPositionsResponse.fx_available + SimExec 删 0 占位启发式 + freshness 删死分支 + ledgerDate 移 format.ts + FreshDot 共享组件；F7 保持（D# rationale）；设计回归（#59 useSim 提升 → 下沉）留痕；对抗测试 + 全量测试/vet/npm build 绿；部署重启实测（covered 18/32 + fx_available=true + bundle 匹配 index-c9bH9JKr.js） |
  | **D-048 总览页布局重构（第一眼原则：确认置顶 + 裁决前置 + 低频折叠）** | ✅ | Collapse.tsx（原生 `<details>` 受控）+ OverviewPage.tsx（ConfirmPanel 置顶整宽 + `.row` 简化双栏 + Insights 提序 + hasKnowledgeMatch）+ Opportunity.tsx（实算卡置前 + 四块 Collapse，funding 默认展开）+ Triggers.tsx（active 顶部 + 全量折叠）+ KnowledgeBoard.tsx（defaultOpen prop）+ style.css（.collapse 系列，删 .row-col/span 2）；锚点恒绿；部署实测 index-DTovgGTO.js served 匹配（对话 #66） |
  | **D-050 总览页布局第 2 版（3×2 网格，业主指定）** | ✅ | MarketMatrix.tsx（矩阵拆分全展开）+ Opportunity.tsx 实算卡瘦身 + OverviewPage.tsx（grid 6 项 auto-flow：矩阵/经验库/机会/确认下单/建议/触发器）+ 告警流移铃铛（Bell 未读数徽标 + 抽屉时间线+ack，删 Alerts.tsx + `.timeline` 死 CSS + OverviewPage 删 ack/ackBusy props）+ style.css（.grid 3fr/2fr + 860px 折叠单栏）；锚点恒绿 + 全量测试/vet/npm build 绿；部署实测 inode 21244337→21244374 + healthz ok + served bundle == 新 dist（index-Bkl8ybxZ.js）（对话 #68） |
  | **D-051 移动/小屏自适应（600px 断点 + 两实测 bug 修复）** | ✅ | style.css 追加 600px 主适配（触摸目标 ≥44px / body 15px / 输入 16px iOS 防缩放 / 表格惯性滚动 / 卡片留白 / safe-area）+ 480px 台账单栏 + hover:none 按压反馈 + button/collapse-summary 的 touch-action/tap-highlight + 修 860px `.grid` `minmax(0,1fr)`（1fr 隐式 auto 最小被矩阵撑破视口 594px）+ 480px `.ledger-form .ledger-note` 特异性（不敌基础 span 2 撑隐式列 511px）；无头 chromium CDP 实测三档视口全过（375/768/1280，无溢出 + 触摸目标 + 桌面零污染）；锚点恒绿 + 全量测试/vet/npm build/tsc 绿；部署实测 inode 21244397 + served bundle == 新 dist（index-CRNqUktn.js / index-Cjd-E_-V.css）（对话 #69） |
  | **D-052 长卡片全局高度协调 + 确认下单提上去** | ✅ | style.css `.scroll-cap`（max-height:min(68vh,520px)+overflow-y:auto，600px 内 min(52vh,360px)）接入机会面板/经验库/进化建议三处列表 + 矩阵表 440px 封顶护栏 + Opportunity/KnowledgeBoard/Insights 三组件接类 + OverviewPage DOM 重排（确认下单第4→第2，桌面右上1/移动第2）；实测机会面板 3092→618、页高 4781→2305、375 无溢出；锚点恒绿 + 全量测试/vet/npm build/tsc 绿；部署实测 inode 21244415→21244416 + served bundle == 新 dist（index-DQsLtvVN.js）（对话 #70） |
  | **D-053 布局第 3 版（右1 双卡堆叠等高矩阵 + 进化建议右2 等高机会面板）** | ✅ | style.css `.right-stack`（flex column gap:16px min-height:0）+ 删 `.grid` 的 `align-items:start`（恢复默认 stretch → 同排单元格沿行高拉伸）+ `.grid > .card` 选择器扩 `.grid > .card, .grid > .right-stack` + OverviewPage DOM 序 = 矩阵 → right-stack(经验库+确认下单) → 机会 → 进化建议 → 触发器；实测 1280 matrix y121 h875 == right-stack h875（经验库 603+确认下单 119 @y755）、opp h618 == insights h618、trig y1645、375 单列无溢出；D-054 加复核按钮后复测仍 875==875/618==618（scroll-cap 封顶防撑破）；锚点恒绿 + 全量测试/vet/npm build/tsc 绿（对话 #71） |
  | **D-054 经验库人工复核（「待复核」闭环）** | ✅ | proto ReviewKnowledgeEntry（signature/status/verdict/validation_note）+ buf 再生成 + store.ReviewKnowledgeEntry + pgstore UPDATE（validated_at=now/status/verdict COALESCE(NULLIF..)/validation_note，未知签名 ErrNotFound）+ dashboard 服务校验（sig 必填/note 必填/status 三态白名单）+ knowledge 状态常量 + 6 处 fake 对齐 + hooks review 增 status 参数 + KnowledgeBoard ReviewForm（状态 select 生效中/已更新/已撤销 + 判定文本留空=保留 + 必填结论 + 确认/取消）+ style.css .review-form 系；对抗：pgstore roundtrip（删 validated_at 子句必红）+ service 校验（删白名单必红）+ 锚点恒绿；部署实测 RPC 三条拒绝路径（空 note/非法 status 400、未知签名 503）+ CDP 复核表单渲染（对话 #71） |
  | **D-055 模拟盘平仓 + 浮动收益列（对称补全写路径）** | ✅ | migrations/0008_sim_close.sql（sim_orders.status CHECK +closed）+ store.go（SimLegClose/SimStatusClosed/CloseSimOrder 接口）+ pgstore/sim.go CloseSimOrder（单事务 filled 守卫→逐腿 pnl+=AddPnl+settled（腿须属本单且 open 任一 miss 回滚）→订单 closed+note，[对抗锚点] 删 filled 守卫/删腿 open 守卫 → TestCloseSimOrder 必红）+ sim_test TestCloseSimOrder + migrate_test want 7→8 + sim.proto CloseSimOrder（头注释 4→6 RPC）+ `buf generate --template buf.gen.sim.yaml` + service.go CloseSimOrder（status != filled → FailedPrecondition 双层防重复平 + latestValue 浮动 AddPnl + fx 折算，包 doc 5→6 RPC 双写路径）+ service_test fakeStore（ListOpenSimPositions/CloseSimOrder 真语义）+ 5 处 fake panic stub + 6 场景服务测试 + hooks useSim.close（返回响应含 realized_pnl）+ SimExec.tsx（浮动收益列 + rowSpan 平仓按钮二次确认 + 结果横幅 + settled 过滤）+ SimPage 传参 + style.css .btn-close；部署实测 healthz pending_migrations→ok + inode 21244442→21244499 + served bundle == 新 dist（index-EdR3ou5I.js 含「确认平仓/浮动收益/closeSimOrder」）（对话 #72） |
  | **D-056 Part A 模拟账户完整现金账本（业主「按真实账户对待」）** | ✅ | migrations/0009_sim_cash.sql（sim_account 单账户 id=1 CHECK + sim_cash_flow 逐笔流水，IF NOT EXISTS）+ store.go（SimAccount/CashFlow 类型 + CashKind 4 常量 + SimLegClose.CashDelta + SettleSimPosition→SettleSimPositionFunding 改名 + InitSimAccount/GetSimAccount/ListCashFlows 接口）+ **pgstore/sim_cash.go（现金账本独立文件：applyCashFlow/applyOpenCashFlows 公共 helper + SettleSimPositionFunding/InitSimAccount/GetSimAccount/ListCashFlows）** + pgstore/sim.go（AcceptSimOrder/FillSimOrder 两成交路径 open 入账口径一致 + CloseSimOrder 逐腿 close 入账，均调 sim_cash helper）+ sim/backfill.go SettleFunding 改调 SettleSimPositionFunding + main.go 启动 seed InitSimAccount(simCfg.Capital)（幂等重启不重置 cash）+ sim.proto CashFlow/GetSimAccount + `buf generate --template buf.gen.sim.yaml` + **simapi/account.go（GetSimAccount RPC 独立文件：双恒等式 equity=cash+market_value=capital+realized+unrealized 交叉校验 + 最近 100 流水）** + simapi/service.go CloseSimOrder 服务端补 CashDelta + 7 处 fake + 对账/原子性对抗测试 + hooks useSim.account + SimExec.tsx AccountZone（五数对账网格 + 净值 USD/RMB + 逐笔流水表，账户区置顶第一眼）；部署实测 seed 100000/100000 + 重启幂等 1 条 capital_in + GetSimAccount RPC 双恒等式对平 + 历史对冲单 pnl 净 0 无漂移（对话 #73） |
  | **D-056 Part B 秒级实时报价（上游 WS / 下游 SSE，业主「秒级实时」）** | ✅ | internal/quote 新包（hub.go 快照+订阅 + binance.go USDT-M 合流 miniTicker 公共行情 + okx.go v5 tickers + sse.go Handler 1s 差分推送 + feed.go wsDial 8s 拨号上限/45s 读空闲/retryLoop 指数退避自愈）+ gorilla/websocket v1.5.3 唯一新依赖 + main.go 装配 hub + /quote/stream + 前端 hooks useQuotes（EventSource 自动重连）+ QuoteStrip.tsx 顶部报价条（binance/okx 双 venue 秒级跳动）+ OverviewPage 挂 QuoteStrip；**根因修复 binance 静默丢帧**（Go json 大小写不敏感匹配致帧内 `"e"` 撞 EventT → 补显式 EventType 字段，测试用真实帧形 envelope+e/E 并存锚定，删字段必红）；部署机实测两 venue 双流（binance BTC 62956.9/ETH 1879.79 + okx BTC 62965 秒级推送）（对话 #73） |
  | **D-057 事故修复：testPool 安全闸 + InitSimAccount 补正 + sim 账本复位（测试误清真实库）** | ✅ | pgstore_test.go `testPool` 守卫（DSN dbname 不含 "test" → t.Fatalf，负测零触碰）+ sim_cash.go `InitSimAccount` 补正（ON CONFLICT DO UPDATE CASE capital=0 + funded 检查幂等）+ sim_test.go `TestInitSimAccountRepairsUnfunded`（删补正/删入金必红）+ 业主确认清理测试残留（保留订单 18）+ 重启 seed 100000/100000 + 1 capital_in 幂等 + GetSimAccount RPC 对平（对话 #75，inode 21244553） |
  | **D-058 盈利验证优先路线（阶段 0 模拟盘盈利性验证运行，testnet 降级可选预演）** | 🔄 | D-058（decisions.md）+ 阶段 0 = D-041 演练档订单持续产生 paper PnL（D-056 账本 + D-055 平仓 + D-037 真实费率结算）+ 判定门①（跨 30 天 paper 净年化 ≥ 诚实基线 3.2–3.7% + 摩擦余量才进阶段 A，为负/不足止投）；运行期定「跨窗口 paper 收益」测量口径 |
  | **D-059 复核自动证据 + 平仓卡 + 订单历史（业主三条 UI/复核需求，对话 #80/#81）** | ✅ | proto KnowledgeEntry.current_evidence + dashboard/knowledge.go `knowledgeEvidence`（evidenceSpikeTrap/DefiSpike/CrossVenue 三纯函数复用 D-046 探测器口径）+ `latestByVenueSymbol` 共享（P3）+ ListKnowledgeEntries 编排（故障降级/缺数据不编造）+ 7 证据测试（fakeStore factsErr 独立故障域）+ ClosePanel.tsx（整单平 armed 二次确认 D-019）+ ConfirmPanel/ClosePanel `.scroll-cap` + OrderHistoryZone.tsx（六态 + rejected 拒单原因）+ sim.tsx `statusText`（practices #18 六态全集）+ KnowledgeBoard 证据行 + ReviewForm note 自动预填（人工在环才写 validated_at）；部署实测 served bundle == dist（index-DRVZi1Rk.js）+ live evidence 三条正确（spike 命中 ×2.9 / cross-venue 命中 7.5pp / defi 未命中 4.39%）+ 拒单 18 负样本在库；零执行门禁改动 |
  | **画布加宽 + 右列间距/顺序 + 复核表单布局（业主反馈，对话 #82）** | ✅ | style.css：`main` max-width 1100→1440px + `.grid` 3fr:2fr→5fr:4fr（右列 ~421→619px、左列 631→773px 无回归）+ `.right-stack > .card{margin-bottom:0}`（双倍间距根因：内部卡 margin 16 + flex gap 16 叠加 32px）+ `.right-stack` gap 16→12px + 复核表单 `110px 1fr auto`（复核结论 span2 + 按钮 col3 同行底对齐 0px，手机 ≤640px 覆盖 span 2 整行）+ `.review-actions` 显式 col3 右对齐（防 auto-placement 塞 110px 变形）；OverviewPage.tsx 交换 ConfirmPanel/ClosePanel = [经验库,确认下单,平仓]（行动优先）；纯前端零后端/RPC/门禁；CDP 实测间距 11/12px + 顺序正确 + 页高 2055→1999 + 375 无溢出；部署 served bundle == 新 dist（index-Cyt2Tw60.js / index-Dok6zbWG.css）+ healthz ok（对话 #82） |
  | **D-060 复核方向快照 + 系统自动翻转检测（业主「没有 claude 怎么做判断」，路线 A，不做 B/LLM）** | ✅ | migrations/0010_knowledge_review_direction.sql（`ADD COLUMN IF NOT EXISTS review_direction text`，幂等约定）+ store.go KnowledgeEntry.ReviewDirection + ReviewKnowledgeEntry 接口加 direction 参 + pgstore UPDATE `review_direction = COALESCE(NULLIF($5,''), review_direction)`（数据面不可判定保留旧快照不覆盖历史）+ List SELECT 加列 + **`var rd *string` NULL 扫描兜底**（真实事故：存量行 NULL → ListInsights 503）+ TestReviewDirectionNullScan 存量回归 + migrate_test want 9→10（消息与条件双改）；dashboard/knowledge.go `evidenceResult{text,hit,known}` + 三证据 helper 改为产出方向（hit/known 生成时定，不反解析文本 P3）+ `recheckNeeded(e,r)` 纯函数（需 ValidatedAt≠nil && ReviewDirection≠"" && known → `r.hit != (e.ReviewDirection=="hit")`）+ ReviewKnowledgeEntry 服务端算方向 + toKnowledgeEntryProto 加 ReviewDirection/RecheckNeeded + proto KnowledgeEntry +13/+14 + buf 双模板再生成 + 对抗测试 TestReviewDirectionSnapshotAndFlip / TestReviewDirectionNotClobbered / TestRecheckNeededGuards + 4 处 fake 签名对齐；前端 KnowledgeBoard `reviewState(e)` 四态（待复核/已复核[无快照诚实不虚称]/仍适用/建议复核）+ 翻转置顶（recheckCount + sorted）+ `.recheck-banner` 警示条 + 按钮「建议复核」recheck-open + 「上次核验 命中/未命中」ts + style.css `.recheck-banner`/`.review-open.recheck-open`；CDP 实测正常态 + 翻转态（Fetch 拦截注入 recheckNeeded，不污染生产库——prod SQL UPDATE 被权限分类器拒未绕过）；零执行门禁改动；部署实测：inode 新二进制 + 健康 200 + ListInsights/ListKnowledgeEntries 双 200 + served bundle == 新 dist（index-_k06EENF.js / index-DyyMgpJU.css）+ prod review_direction 列已迁移（对话 #83） |
- 阻塞/待决策:
  - 无阻塞。TRX 独立处置（业主自定，费率转正触发器已入监控规格）。
  - SMTP 授权码待办**已移除**（D-033：业主不做邮件推送，浏览器铃铛为主通道）。
  - R3-L3 遗留：calendar collector 的 thursday 周记语义待业主决策（低危，接受待决）。
  - **M3-a 已验收（D-035）**；**M3-b 已施工交付**（D-037 + 本 commit，S1–S5 全落地，见施工表）。
  - **S3 已启用**（业主 key 到位，dialogue #48）：/etc/arbcn/arbcn-sim.env（mluser:mluser 0600）；binance/okx 连通实测 200；探针 8h tick 跑，**首次 heartbeat 登记待下一个 tick**（ListSourceHealth 应见 sim_testnet_binance/okx）。服务以 mluser 运行（key 文件属主为 mluser 非 root，运行环境实测修正）。
  - **M3-b 交付注**：sim 已接线进 main.go（backfill / simDriver / OnActive compose / 8h 结算循环 / 探针随 tick）；结算数据源 = 真实市场公开 funding（D-037 裁决），testnet 费率不参与结算只做 key 隔离验证。sim 包保持零网络零密钥（domains_test + TestNoNetworkImports 把关）。
  - **M3-c 交付注**：SimService 独立域已接线 main.go（st 非 nil 即挂载，sim 配置缺失降级：GetSimReport 返回未启用说明，D-032 同口径）。**repo/carry 二次门禁恒拒已由 D-039 裁决修复**：数据面按 kind 分派权威源（repo→reverse_repo 利率、carry→defi_rate 年化、funding_hedge→ticker/funding），权威源查不到仍 fail-closed 拒（不放宽），ConfirmDriftCheck 签名不变。
  - **D-031 实证修订**：data-api.binance.vision 不镜像 /fapi/*（404）；历史回填源回落 fapi.binance.com（部署机直连 200、满 365d）；OKX 历史端点为 funding-rate-history（funding-history 404），仅保留 ~90d（OKX 部分覆盖，sim_report/avg_30d 受窗口限制，已知 degrade）。
  - 待决策观察（不阻塞）：repo 信号经 SignalToOrder 时仍受 5% 门槛（SPREAD_LOW）约束——平时逆回购 2-4% 会被拒单，仅季末/年末上冲 ≥5% 时放行；与"时点逆回购"策略意图一致（宁缺毋滥），但若业主希望 repo 绕过价差门槛须走 D# 调整。D-045 已确认 repo 保持 5%（意图不变）；carry 改用独立 CarryMinSpread=1% 已生效。
  - D-045 交付注：carry 白名单已生产配置（SUSDE/USDE/BUIDL/STEAKUSDC/USDY 大写，config.go 不做大小写归一）；carry 单真正触发依赖 defi_large_tier_change 命中白名单标的（≥0.5%/h 变动），不造数据；repo 因当前 0.865%<5% 仍 SPREAD_LOW 拒（负样本，设计内）。
  - **D-046 交付注**：机会实算卡默认摩擦 = 0.3%（业主已核实普通主户，facts.md 落档）；升 VIP/启用 BNB 抵扣 → 改 env `ARBCN_OPP_FRICTION_FUNDING`，不改代码。经验库 3 条 seed 覆盖已知模式，新情况靠后续 D# 逐步吸收（自我进化机制 = 越用越多，非一次建满）。实算卡评级与执行门禁是两层：卡说「可抓」≠ 自动执行，建单仍须过 D-016 15%/20%、MinSpread/CarryMinSpread、carry 白名单（只读证据不碰门禁）。
  - **D-049 funding 数据源边界（对话 #67）**：维持 binance+okx 两家不加所（数据面已覆盖业主可交易面；瓶颈在标的维度不在所维度——流动性所 funding 趋同，极端 funding 全在微盘陷阱币，对话 #52 实证）。**例外触发** = 跨所费率分歧在流动性标的上反复命中且业主确证两腿可套 → 才考虑加第三所（Bybit，2 所 = 每币 1 分歧对 / 3 所 = 3 对）；拓宽窗口监测优先加流动性中市值标的（SOL/XRP 等，须两所均有现货+永续可对冲 + 实证过门槛，宁缺毋滥）。触发前不建新 collector。
  - **facts.md 快照无界增长（发现于 D-048/D-049 收工核验，待 D# 决策）**：`internal/exporter` 每次导出（boot + 24h 定时 + **每次规则 armed→active 都触发**）追加整份新快照、旧快照标已过期不删除（D-028「不删除」规则）→ facts.md 已达 **1564 行 / 40 个快照（约 1500 行/天增长），远超 T1 ≤450 行预算**。根因 = 快照是 T2 级时间序列历史（只存不读）却写进 T1 事实库，且规则触发导出无节流。修复候选：① 快照段封顶（只留最近 N 份，旧份机械移 LOG.md）——推荐，快照历史本就归 LOG.md；② 快照写入独立历史文件（facts.md 留指针）；③ 规则触发导出节流（≥10min 合并突发）。走 D# 落定后施工 + 部署。
  - **D-056 遗留观察（不阻塞，待 deploy 后观察）**：binance REST ticker 采集（collect/exchange 1min 轮询）在 throwaway 验证时未写入（okx 正常写入）——与 D-056 Part B 的 WS 报价流无关（WS 已实测双 venue 双流），疑是 REST 侧独立问题待部署后看 source health 定位。
  - **D-060 遗留观察（不阻塞，运行态）**：review_direction 快照只对「复核时刻之后」的数据可判方向——复核早于 D-060 的存量条目（如 defi:single_pool_spike）无快照，恒「已复核」不参与翻转检测；要补齐须业主在下次复核时重新提交（会顺带写入新快照）。翻转检测只在当前数据面可判定时工作（数据缺失 = 不误标，宁缺毋滥）。「建议复核」条目的实际复核（是否采纳/改判）仍走人工在环 D#，待业主首次实际用「建议复核」流程时观察顺手度。
- 下一步: **① D-058 阶段 0：模拟盘盈利性验证运行（无 API）**——真实候选（BTC cash-and-carry ~7% 等）经 D-041 演练档持续产生模拟成交 + 8h 真实费率结算（D-037）+ 平仓（D-055），积累 paper PnL；运行期定「跨窗口 paper 收益」测量口径（GetSimAccount 账本跨时点读数）；判定门①：跨 30 天窗口 paper 净年化 ≥ D-026 诚实基线 3.2–3.7% + 摩擦余量 → 进阶段 A 真金冒烟（~1 万 RMB，摩擦后真实盈利唯一裁决），为负/不足即止投（业主原则：模拟亏损 = 路不通）。**①+ 经验库「建议复核」首个真实案例验证**（D-060 运行态：业主用「建议复核」按钮按提示复核翻转条目 → 提交时写入新方向快照 → 一致性恢复「仍适用」；首次真实走通后评估「建议复核」触发是否过频/过钝，再定是否调翻转检测口径）。**② 修复 facts.md 快照无界增长**——真实候选（BTC cash-and-carry ~7% 等）经 D-041 演练档持续产生模拟成交 + 8h 真实费率结算（D-037）+ 平仓（D-055），积累 paper PnL；运行期定「跨窗口 paper 收益」测量口径（GetSimAccount 账本跨时点读数）；判定门①：跨 30 天窗口 paper 净年化 ≥ D-026 诚实基线 3.2–3.7% + 摩擦余量 → 进阶段 A 真金冒烟（~1 万 RMB，摩擦后真实盈利唯一裁决），为负/不足即止投（业主原则：模拟亏损 = 路不通）。**② 修复 facts.md 快照无界增长**（D# 定修复方案：快照段封顶留最近 N + 旧份移 LOG.md 为推荐；exporter 规则触发导出节流；施工 + 部署 + 实测 facts.md 回落 ≤450 行）——facts.md 仍以 ~1500 行/天膨胀破坏 T1 层，紧随阶段 0 之后。③ 延续观察项：**阶段 0 观察**（演练档订单是否持续产生 paper 证据、判定门①测量口径落地）+ **平仓运行态**（业主实际用平仓按钮关单流程是否顺畅、实现净 PnL 横幅数值是否符合预期、settled 腿移出持仓表后是否有历史可查）+ D-053 布局运行态（桌面右1 堆叠卡内滚动滚动条是否明显、确认下单位于堆叠下半首屏可及性、经验库展开复核交互是否顺手）+ D-052 布局运行态（桌面确认下单首行是否顺手、封顶滚动高度是否合适）+ D-051 移动适配真机运行态（手机浏览器 375px 首屏无横向滚动、确认下单单列第2位首屏可及、复核按钮触屏好点）+ 经验库持续吸收（新市场结构情况 → D# → knowledge.Defaults() 加条目 → 部署）+ **复核运行态**（业主实际用复核按钮更新经验库条目的流程是否顺畅、validation_note 留痕是否够用）+ **D-059 复核自动证据运行态**（自动证据 + note 预填复核是否顺手、证据文本（命中/未命中 + 数值 + 阈值）是否够裁决、拒单负样本是否需更多门禁维度）+ **D-059 平仓卡/订单历史运行态**（总览平仓卡是否顺手、与 SimExec 平仓入口冗余是否可接受、订单历史六态展示是否满足复盘）+ **#82 布局运行态**（画布 1440px 宽屏是否合适、右列三卡 12px 间距是否够紧凑、确认下单在平仓上方是否顺手、复核表单按钮与复核结论同行是否够、经验库右列更宽后证据文本是否少换行）。④ 后续审计候选：后端/测试侧第一性原则审计（前端两轮已毕）。L1 候选（数据累积后）：窗口内 max 冲高检测 / 阈值自适应统计自校准 / 拒单-阈值联动归因。
- 清扫上翻: **D-060 复核方向快照 + 不做 B(LLM) 理由入 decisions.md**（判断编码为「复核时快照 + 翻转比较」——系统永不裁决只比较；不做 B 三理由：判断力错配/成本与复杂度/责任不可归属，D-043 缺口维持开放；三角色运营模型——系统 7×24 确定性比较 + 业主二元确认 + 决策层周期会话 D#）；**上线即抓事故教训入 practices #37**（可空列必须 *string/pgtype 兜底 + 存量 NULL 回归测试——fake 无 NULL 单测测不出，browser 实测先暴露 + 「复核快照+翻转比较」确定性编码正解 + 复核类状态区分「无基线」与「一致」诚实标注）；对话落 dialogue.md #83（业主四连概念问 + 路线 A vs B 裁决 + 不做 B 理由确认落档 + 施工 + NULL 事故 + CDP 两态验证 + 部署闭环）；对话落 dialogue.md #84（拒单理由自明性改进 + id=18 历史 note 经业主授权回填 + 订单历史表布局修复后续②③④——理由完整显示/消左空白/桌面重叠/表头居中值左对齐四轮，均 CDP 实测部署闭环）。本次 review 教训入 practices.md #6-#10（刻度统一/NaN 门禁/状态+从属行原子/信任边界标注/时钟注入覆盖全路径）+ #11（统计效力）+ #12（数据源端点必须部署机实测）+ **#13（门禁/折算数据面按实体类型分派，D-039 教训）**；**L0 进化建议引擎入 decisions.md D-044**（四信号只读证据表面 + dashboard 域 RPC + 按需计算 + 中位数×因子 + 动作一律走 D#；L1/L2 候选留给数据累积后）；**洞察/建议引擎边界教训入 practices #20**（只读证据表面永不自动改规则——优化方向由数据提示、优化动作由决策层拍板，"证据面"与"执行面"分离）；对话落 dialogue.md #62（半自动增强确认 + 决策层把关目标修正 + 施工 + 部署实测）；dialogue.md 轮转 #8~#53 至 LOG.md；追溯深审 + M3-a 复审结论入 decisions.md D-035；M3 文档审计结论入 D-036（收敛口径修正 + G1–G5）；M3-b 细化设计入 D-037（spec §9 + 结算数据源裁决）；S4 数据源实证修订入 D-031（data-api 前提否定 + fapi 回落 + OKX 端点修正）；M3-c 细化设计入 D-038（spec §10 C1–C5）；**M3-c 复审修复（repo/carry 恒拒）入 D-039**；S3 启用 + OKX 探针 ts 格式教训入 practices #12 + dialogue #48；**D-040 测试网账户区入 D-040**；跨数据源同名数值口径标注教训入 practices #14；对话落 dialogue.md #39~#55（含 #51 轮询 vs 事件驱动架构确认、#52 机会盘点 + 微盘陷阱实证、#53 OTC 法币通道实测通过、#54 告警流高度封顶修复、#55 模拟执行无机会核查 + 演练档选型）；**D-041 演练档入 decisions.md**（funding_drill band [5%,15%) 设计 + 告警 Info 级 + 复用 fundingHedgeSignal + 对抗测试锚点）；**D-042 拒单根因修复 + boot 加固入 decisions.md**（SQL 优先级 bug 根因 + TestLatestFactsFilters + BootDelay + TestRunBootDelay + migrate 修正）；**经验教训入 practices #15**（/proc/PID/exe 与磁盘文件对比须用 `stat -L`，procfs 伪 inode 曾误导"旧二进制"假象；SQL where 拼接每子句必须加括号防 AND/OR 优先级陷阱）；对话落 dialogue.md #56/#57/#58；**前端二次确认按钮 bug 教训入 practices #17**（disabled 绑 pending 会禁掉第二次点击，须绑 busy）；**SimExec 三处体验修复（提示条关闭入口 / 风险标记中文全量映射 / 持仓实时数值）入 dialogue #58**（口径：实时收益=已结算+未实现、预期收益=预期年化%、提示条手动 × 关闭；unrealized 只依已确认实时价计算，行情缺失不编造浮动）；**教训入 practices #18**（前端枚举值映射表必须对照后端常量全集，别只映射见过的——riskLabel 曾漏 5/7 个 Risk 常量显示英文原文）；**确认下单面板移入总览页入 dialogue #59**（业主决策：简化只留待确认+确认按钮；sim tab 保留持仓/账户/报告；useSim 提升 App 层共享 + sim.tsx 抽共享，锚点扩展）。**确认下单卡片布局调整（与机会面板同行）+ 交付闭环补课入 dialogue #60**（端到端验证：订单 id=5 suggested→确认成交，持仓对冲相消=0）；**教训入 practices #19**（§7.3 自审 ≠ 交付终点：运行态改动 check-out 必须「构建 → 部署应用 → 推送」三步闭环，本地 commit 不代表上线——业主"提交推送应用"触发本次补课）。**业主三问答入 dialogue #61**（defi_rate 五项 = DeFi 协议资金池非交易所、Aave USDC 12.57% 异常值、矩阵滑块根因修复、实盘操作流程、LLM 接入裁决=不建议）；**LLM 方向锚定落 D-043**（暂不接入，未来解读用模板叙述；业主认可，方向记录在案）。**D-045 carry+repo 接入入 decisions.md**（结算数据面按腿 kind 分派 + repoSignal venue 真实值 + CarryMinSpread 门槛分档 + 白名单显式配置；1% = 纠正口径错配非放宽门禁）；**教训入 practices #21**（施工前必须先对设计文档做 A–F 自审——自行施工/分发施工都须，自审是施工前置门禁不是汇报材料，不待业主追问；AGENTS.md §7.3 已加「两时点强制」条款）+ **practices #22**（结算/折算数据面按实体 kind 分派 + 落单 venue/symbol 取事实真实值勿硬编码，practices #13 结算侧延伸）；**部署发现**：/etc/arbcn/arbcn-monitor.env 此前不存在（systemd `EnvironmentFile=-` 全默认运行），本次新建仅含白名单一行；对话落 dialogue.md #63（全面模拟决策 + 施工前自审纪律确认）。**D-046 机会实算卡 + 经验库入 decisions.md D-046**（确定性算账公式 + 三档判定 + 摩擦已核实 + 经验吸收闭环 = 人工 D# / 匹配纯函数 / 呈现只读 / 永不自动吸收；实算卡只读不碰执行门禁；诚实标注 3 条 seed 覆盖已知模式，新情况靠 D# 逐步吸收）；**教训入 practices #23**（「算账/匹配是公式不是聪明，写死进系统；经验吸收=人工 D# 落盘、匹配=纯函数、呈现=只读，永不自动吸收；NaN 不可用 0 占位——NetAnnualized 均值不足须显式 math.NaN()，0 会被判定误读为净≤0 坑」）；**friction 0.3% 已核实入 facts.md**（普通主户双 taker，对话 #64，后续改 env 不改代码）；对话落 dialogue.md #64（投运后自算账核心追问 + 自我进化方向定案 + 摩擦核实）。**前端第一性原则审计修复入 decisions.md D-047**（P0 数据 hook 随视图生命周期 + P1 映射收敛 kindText/covered 单源 + P2 小项；设计回归留痕：对话 #59 useSim 提升 → 本次下沉，SimPage 挂载即重拉天然覆盖；F7 保持 rationale）；**教训入 practices #24**（数据 hook 必须随视图生命周期挂载不绑 App 根；「刷新」必须覆盖它能刷的全部数据源（refreshKey 信号机制）；缺失信号用 presence flag 别用 0 占位——pnl_rmb=0 当汇率缺失是 0-as-missing 二犯）；对话落 dialogue.md #65（审计结论 P0/P1/P2 + 拍板全修 + 施工 + 部署实测）。**D-048 总览页布局重构入 decisions.md D-048**（UI 第一性原则 = 用户第一眼问题层级决定布局；U1 确认置顶 / U2 裁决前置 + 数据折叠 / U3 经验库命中才展开 / U4 active 信号面；折叠 = 原生 `<details>` 受控 Collapse 三处复用；删 .row-col/span 2 死样式；纯前端零门禁改动）；**教训入 practices #25**（页面布局由用户第一眼最需要看到什么决定，按第一眼问题层级排序——行动/裁决/信号先于数据下钻/参考面，低频参考面折叠不丢数据面）；对话落 dialogue.md #66（UI 第一性原则方向 + 审计三处违背 + 业主拍板全量重构 + 施工 + 部署实测）。**D-049 funding 数据源范围裁决入 decisions.md**（不加所 + 例外触发条件——跨所分歧在流动性币反复命中且可套才加 Bybit / 扩窗口优先加流动性中市值标的，受宁缺毋滥约束；纯数据源范围零代码改动）；对话落 dialogue.md #67（业主问「加不加更多交易所」→ 裁决维持 binance+okx 两家）；facts.md 落档「funding 数据源边界」现行（决策前先查库 D-028）。**收工核验发现 facts.md 快照无界增长**（40 快照 / 1564 行 / ~1500 行每天，根因 = exporter 每次规则触发导出追加不删 + 快照是 T2 级历史却写进 T1 文件）——已落档 STATE 待决策（修复候选 ① 封顶移 LOG.md / ② 独立历史文件 / ③ 触发节流），留待 D# 定案施工，不作为本会话交付范围。**D-050 布局第 2 版入 decisions.md**（3×2 网格业主指定 + 信息架构/操作热区第一性 + 告警流状态优先移铃铛 + MarketMatrix 拆分 + D-048 U2 反转留痕；纯前端零门禁改动）；**教训入 practices #26**（布局第二课：眼睛扫描起点=左上（数据监控置顶）+ 鼠标顺手高度=中右（操作热区）+ 告警流第一性=状态优先（徽标/计数）内容拉取式（时间线+ack 进抽屉）；组件拆分让卡片变短可扫）；对话落 dialogue.md #68（业主明确 3×2 网格指令 + 「打开首页第 1 版就能完成基本的信息和操作」总则 + 告警流第1性原则推导 + 施工 + 部署实测）。**D-051 移动/小屏自适应入 decisions.md D-051**（600px 主断点/480px 表单/触摸目标 ≥44px/iOS 输入防缩放/惯性滚动/safe-area/hover:none 按压 + 375px 实测抓两真 bug：grid `1fr` 隐式 auto 最小撑破视口 + ledger-note 媒体特异性；纯 CSS 零门禁）；**教训入 practices #27**（grid 轨道 `minmax(0,1fr)` 防 min-content 撑破 / 媒体查询覆盖规则特异性必须 ≥ 基础规则 / 移动布局必须真实视口实测（375px docScrollWidth 对比 innerWidth）替代截图推断 / 断点对齐既有 + 桌面回归）；对话落 dialogue.md #69（业主指令「需要做」+ 审计结论 + 施工 + 三档视口实测 + 两 bug 修复 + 部署闭环）。**D-052 长卡片高度协调 + 确认下单提上去入 decisions.md D-052**（.scroll-cap 卡内滚动代替分页 / 矩阵封顶护栏 / DOM 重排提权 = 确认下单右上1·移动第2 / 待复核永不自动已核实——pgstore 证据；页高 4781→2305 实测）；**教训入 practices #28**（长列表高度封顶卡内滚动防无限延伸 + DOM 序=布局序重排即提权 + 业主观察常在移动单列序 + 「永不自动」类问题先读存储层代码再答）；对话落 dialogue.md #70（五连问核实 + 决策 + 施工 + 1280/375 实测 + 部署闭环）。**D-053 布局第 3 版 + D-054 人工复核入 decisions.md**（.right-stack 单 cell 等高 = grid 默认 stretch 零逐卡规则 / 复核 RPC 语义 = signature 必填 + status 三态白名单 + verdict 留空 COALESCE 保留 + note 必填留痕、只改判定记录不触门禁、verdict/status 绝不混写——初稿把枚举写进 verdict 列自审纠正）；**教训入 practices #29**（等高/等宽先试容器默认拉伸（grid stretch/flex）别逐卡硬编码，堆叠列记得 min-height:0；「复核/确认」= 人工在环 HITL 系统永不自动复核，状态枚举与自由文本判定分开设计勿塞一列，留空保留语义用显式分支 COALESCE，UI「待复核」状态必须存在可达写路径否则是死状态）；对话落 dialogue.md #71（布局指定 + 如何复核 + 施工 + 1280/375 实测 + RPC 拒绝路径 live + CDP 表单渲染 + 部署闭环）。**D-055 平仓 + 浮动收益列 + 预期收益答疑入 decisions.md**（订单级整单平绝不单腿 D-019 + AddPnl=当前价浮动复用既有口径 + 双层防重复平 + 迁移 0008 + 前端浮动列/二次确认按钮/settled 移出持仓表；expected_ann 是实时投影非锁定值的答疑，facts.md 证据 funding 8%→4.9% 市场条件变化非 bug）；**教训入 practices #30**（功能「只做一半」补全先核对对称侧写路径全表（迁移→store→RPC→fake→前端）再动工，整单平 vs 单腿平先想语义再写守卫；buf 多模板项目改哪个域 proto 用哪个模板——裸 `buf generate` 不碰 sim 域，再生成后 grep 新符号确认落入生成物）；对话落 dialogue.md #72（平仓补齐 + 浮动收益列 + 预期收益答疑 + 施工 + 部署实测 healthz pending_migrations→ok + served bundle == index-EdR3ou5I.js）。**D-056 现金账本 + 实时报价入 decisions.md**（Part A 完整现金账本 = 业主「按真实账户对待」：sim_account/sim_cash_flow + 四 kind 事件全在既有事务内原子入账 + equity 双恒等式一套账两种展开 + 历史对冲单 pnl 净 0 无漂移；Part B 秒级实时报价 = 上游 WS/下游 SSE 选型 + 只展示不喂策略不落库 + 无密钥 D-010）；**教训入 practices #31**（Go json 解码字段名大小写不敏感匹配——帧内同时有 "e" 字符串与 "E" int64 时漏声明 lowercase 字段会让它撞上 int64 字段致整帧 Unmarshal 失败，binance 系帧必须显式声明全部字段；测试用真实帧形（envelope + e/E 并存）锚定，删字段必红）；**教训入 practices #32**（「启动即 seed 幂等写」类基建要接线 main.go 而不是当死代码留着——InitSimAccount 此前从未被调用 = 资本从未 seed；seed 须单事务（半账修复）+ fail-fast + 重启不重置 cash 跨重启持久）；**端点教训续 practices #12**（binance WS 同样 geo-block：fstream.binance.com 部署机 i/o timeout，fstream.binancefuture.com 直连通——与 REST fapi→fapi.binancefuture 同源，WS 端点同样必须部署机实测）；对话落 dialogue.md #73（业主两条需求「按真实账户对待」+「秒级实时报价」+ 选型答复 + 施工 + live 核账 + 部署机双流实测）。**D-057 测试事故修复入 decisions.md**（test DSN 误配清库事故全链路 + testPool 安全闸 + InitSimAccount 补正；教训：go test 真库集成测试必须指向专用测试库、全表 TRUNCATE/DROP 的测试基建必须带安全闸、applyCashFlow 兜底建行 vs InitSimAccount 幂等的设计缺口）；**教训入 practices #33**（pgstore 集成测试会对目标库 TRUNCATE——`ARBCN_TEST_PG_DSN` 必须指向含 "test" 的专用测试库，跑测试前先验 DSN dbname；测试残留会伪造出真实症状——sim_account capital=0 卡死曾被误判为生产 bug，先查「最近的 go test 是否打了这个库」再下根因结论）；对话落 dialogue.md #74/#75/#76（确认下单单行摘要删减方案 / 测试事故报告 + 修复 + 清理复位 + 部署闭环 / 业主确认「只用 binance+okx 报价接口、无任何交易接口（含模拟）」，simtestnet 探针仅只读余额）。**D-058 实盘推进路线入 decisions.md**（盈利验证优先：阶段 0 本地模拟盘盈利性验证 → 判定门①（30 天窗口 paper 净年化 ≥ 诚实基线 + 摩擦余量，为负/不足止投）→ 阶段 A 真金冒烟 ~1 万 → 阶段 B/C 按证据放大；testnet 从必经降为可选预演，非必经随时可补；D-010/D-034 零变更——testnet key 只读探针不扩下单，真金零密钥；testnet 执行层设计文档不写，待判定门①通过复议）；**教训入 practices #34**（「第 1 原则是盈利」先于执行机制——验证执行机制的价值是让真金测试不被执行层 bug 污染，而非代替盈利验证；testnet 摩擦假验证不了盈利能力，盈利只能靠真金冒烟 + 本地模拟盘真实费率结算）；对话落 dialogue.md #77-#79（实盘距离核实 + 推进建议 / testnet 可行性 + 起草授权 / 盈利意义澄清 + 两次纠偏 + 方向确认）。**D-059 复核自动证据 + 平仓卡 + 订单历史入 decisions.md**（自动证据+人确认 = 复核类 UI 正确形态——系统算当前数据证据人工点确认才写判定，证据缺失降级不编造不阻断浏览；ClosePanel 整单平 + scroll-cap；OrderHistoryZone 六态 + 拒单负样本保留原因；零执行门禁改动）；**教训入 practices #35**（自动证据+人确认 = 复核/审批类 UI 的正确形态——给证据别给教程；证据与判定两层状态系统永不自动改判；辅助数据面故障降级不阻断主浏览；证据文本含阈值与关键数值）；对话落 dialogue.md #81（三问 + AskUserQuestion 确认「自动证据+人确认」「模拟执行 tab 订单历史」+ 施工 + 部署实测 live evidence + 拒单负样本）。**#82 布局教训入 practices #36 扩展（④ 双倍间距 = 子元素 margin + 容器 gap 同向叠加，清零外边距要覆盖到容器内部子元素层级，CDP 测 cardGaps 比看样式值可靠；⑤ 表单工具栏行对齐用 align-items:end + 显式 grid-column，改网格列数/断点必须同步核对每个显式列号子项——桌面 col3 手机 span 2 的覆盖最易漏）**；对话落 dialogue.md #82（画布加宽 + 复核按钮变形 + 右列双倍间距/顺序对调 + 复核按钮同行 + CDP 1280/375 实测 + 部署闭环）。
